# セキュリティ

## 脆弱性の報告

**公開 Issue には書かないでください。**

このリポジトリの [Security タブ](../../security/advisories/new) から
非公開で報告できます（GitHub Security Advisory）。受け取ったら数日以内に
一次返答します。個人が趣味の時間で見ているので、平日夜と週末が中心です。

英語でも日本語でもかまいません。
*Reports in English are welcome — please use the Security tab above,
not a public issue.*

## 対象バージョン

**最新のリリース**だけを見ています。このリポジトリの `main` は常に最新リリースの
中身で、**コミット名がそのままバージョン名**です（履歴の一番上を見てください）。
古いバージョンへの修正の後追いはしません。

## このアプリの前提

Bishudeck は **他人が書いた HTML をそのまま開いて編集する**ツールです。
取り込む HTML は信用できないものとして扱う、というのが設計の出発点で、
境界は次の 4 つに置いています（詳細は
[docs/basic-design/10-security.md](docs/basic-design/10-security.md)）。

- **編集モードで JS を実行しない。** 編集用の iframe に `allow-scripts` を付けない
- **プレビューに `allow-same-origin` を付けない。** スクリプトを動かす側には同一オリジンを渡さない
- **Rust 側は HTML を解釈しない。** ファイル I/O・アセット配信・プレビュー公開・ウィンドウ管理だけを持つ
- **権限は最小限。** Tauri の capabilities はダイアログとウィンドウのみで、`fs` も `shell` も渡していない

この境界を越えられるもの（編集中のデッキでスクリプトが動く、プレビューが
同一オリジンを取る、`slides://` がアプリの外のファイルを配る、書き出しが
意図しない場所に書く、など）は脆弱性として扱います。

秘密情報は扱っていません。API キーや認証情報をアプリに渡す経路は
いまのところありません。

## 報告に含めてほしいもの

- 再現手順（可能なら、再現する最小の HTML）
- OS とバージョン、アプリのバージョン
- 何が起きるか（どの境界が越えられるか）

**再現用の HTML を送るときは、中身に機密が入っていないか確認してください。**
