---
feature: asset-pipeline
status: active
updated: 2026-08-23
---

# asset-pipeline — 判断ログ

蒸し返し防止用。ここは「まだ仕様に昇格していない決定」と「経緯」の置き場。仕様の正は design.md。
全体に影響する判断はここではなく [../../adr/](../../adr/) へ。

## 記録基準

- **書く**: 議論して決めたこと / 代替案を退けたこと / 後で「なぜこうした？」になりそうなこと
- **書かない**: 自明な判断、コードを見れば分かること

## 運用

- 判断が覆ったら行は消さず、状態を `上書き → #n` にする（覆った経緯こそ蒸し返し防止に効く）
- 判断が仕様として安定したら **design.md 本文に反映**し、状態を `反映済` にする。AI が普段読むのは design.md 側で、ここは「なぜ？」と遡るときだけ読む

## ログ

| # | 日付 | 判断 | 理由 | 見送った代替案 | 状態 |
|---|---|---|---|---|---|
| 1 | 2026-08-23 | markup に書くのは**相対パス `assets/<名前>`** で、`slides://` は書かない | 書き出した HTML はそのまま他のブラウザで開ける必要がある(不変条件 2)。エディタ固有のスキームを書くと壊れる。相対パスならプレビューと書き出しは何もしないで成立し、手当てが要るのは `srcdoc` の編集ステージだけになる | `slides://` の絶対 URL を書く(書き出しが壊れる) / data URL に埋める(HTML が肥大し、往復で太る) | 反映済 |
| 2 | 2026-08-23 | **バイト列はバックエンドから出さない。** フロントが持つのは名前だけ | 数十 MiB の配列を IPC で往復させる理由が無い。フロントに要るのは「どう参照するか」だけで、中身は要らない | フロントで ArrayBuffer を保持する(メモリを二重に食い、IPC のコストも払う) | 反映済 |
| 3 | 2026-08-23 | `assetBaseUrl()` は**起動時に 1 度取って同期で読む** | ドキュメントの合成はレンダリング中に走るので IPC を待てない。届く前は base 無しで描くが、影響を受けるのは取り込んだファイルを参照するデッキだけ | 合成のたびに await する(レンダリングが非同期になる) | 反映済 |
| 4 | 2026-08-23 | 参照されなくなったアセットは**掃除しない**(既知の妥協) | どのアセットが生きているかは HTML 文字列を解釈しないと分からず、それは Rust 側の責務ではない(不変条件 9)。フロントで参照を数える設計は将来検討 | 書き出し時に HTML を走査して未参照を落とす(Rust に HTML の知識が入る) | **未対応** |
