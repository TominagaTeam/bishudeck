---
feature: editing-engine
status: active
updated: 2026-08-26
---

# editing-engine — 判断ログ

蒸し返し防止用。仕様の正は [design.md](design.md)。
全体に影響する判断は [../../adr/](../../adr/) へ。

## ログ

| # | 日付 | 判断 | 理由 | 見送った代替案 | 状態 |
|---|---|---|---|---|---|
| 1 | 2026-08-20 | **複数選択を取り下げる**(`uids: string[]` → `uid: string \| null`) | インスペクタ・座標パネル・文字書式はどれも先頭の uid しか見ていないので、2 つ目を選ぶと**枠は増えるのに書式変更が何も効かない**状態になっていた。「選べるのに何もできない」は、選べないことより分かりにくい | 下流を全部直して本当の複数選択にする(別作業として切り出した) | 反映済 |
| 2 | 2026-08-20 | スライド大の要素かどうかを**面積比(95%)ではなく辺ごとの距離(4%)**で判定する | `inset: 24px` のラッパーが面積比では 89.8% にしかならず素通りしていた。辺の距離ならアスペクト比にも依存しない | 面積比のしきい値を下げる(アスペクト比依存が残る) | 反映済 |
| 3 | 2026-08-20 | 背景から除外するのは「子要素を持たないもの」ではなく**「それ自体が中身を持つもの」** | 判定を反転させていた。`position:absolute; inset:0` の空の `div` で背景を描くのは AI 生成デッキの定番で、余白のどこをクリックしても最初に当たるのがその要素だったため、実質「常にスライド全体が選択される」状態だった | 子要素の有無で判定する(反転していた) | 反映済 |
| 4 | 2026-08-20 | 塗りは色見本だけでなく**「透明 / 単色」の種類を先に置く** | 色入力は「塗りなし」を表現できない。塗りの無いボックスの `background-color` は `rgba(0,0,0,0)` に計算され、色見本はそれを黒としか表示できない — 塗っていない箱すべてが「黒で塗ってある」ように見えていた | 色見本だけを置く | 反映済 |
| 5 | 2026-08-20 | 塗り・枠線とも**「なし / 透明」と「解除」を分ける** | デッキ自身の CSS が塗っている / 描いている場合、宣言を消すとそれが戻ってくる。「なし」は上書きとして書いて元の CSS に勝たせ、「解除」は上書きをやめて本来の見た目に戻す | 1 つのボタンで兼ねる(どちらの意図か表現できない) | 反映済 |
| 6 | 2026-08-21 | 枠線は `border` ショートハンドを書かず、**3 つのロングハンドを毎回書く** | ショートハンドは `border-image` まで初期化するので、デッキが使っていた場合にユーザーが触っていないものを壊す。また部品は独立していない(`border-style: none` のままでは色だけ変えても見えない) | ショートハンドを書く／触ったプロパティだけ書く | 反映済 |
| 7 | 2026-08-21 | 枠線の太さは **input のたびに適用**する(遅延コミットしない) | 太さは「見て決める」値で、Enter を押すまで結果が見えないのでは判断できない。`tryMerge`(800ms)があるので 1 undo ステップに収まる | blur / Enter で確定する | 反映済 |
| 8 | 2026-08-21 | 位置・サイズの数値入力は**対象の uid と基準値を focus の瞬間に ref へ退避**し、確定はその退避値に対して行う | blur を起こすのはたいてい「別のボックスをクリックした」操作で、その時点では pointerdown ハンドラが選択を切り替えている。確定時に「今の選択」を読むと **A に入力した数値が B に適用される**(実際に起きた) | 確定時に現在の選択を読む(不具合の原因) | 反映済 |
| 9 | 2026-08-21 | computed の読み取りには**読んだ要素の uid を添える** | 読み取りは effect なので、選択が変わった直後の 1 レンダリングは前の要素の値を持つ。「与えられた値を覚える」フィールド(色見本)は前の要素の色を握ってしまう | 読み取り結果をそのまま使う | 反映済 |
| 10 | 2026-08-19 | リサイズは**予測せず実測する**(`resizeKeepingAnchor`) | サイズを書いたとき要素がどれだけ動くかは、要素ではなく周囲のレイアウトの性質で決まる(`left/top` 固定・flex 中央寄せ・通常フローで全部違う)。予測すると必ずどれかで外れ、掴んでいない側の角が伸びの半分だけ動く | レイアウト種別ごとに補正式を持つ | 反映済 |
| 11 | 2026-08-19 | **インライン編集の出口はホスト側の要素**(シールド・ツールバー・パネル・余白)にする | WebKit はスクリプト無効 document では、親フレームから `addEventListener` したリスナすら発火させない。iframe 内の Esc に頼ると **Chrome ベースの smoke だけ通って実アプリで壊れる** | iframe 内の keydown で閉じる(WKWebView で効かない) | 反映済 |
| 12 | 2026-08-19 | オーバーレイの追従は**イベントではなく MutationObserver** | 同上。MutationObserver はイベントではないのでスクリプト無効 document でも記録される | frame の input イベントを聞く | 反映済 |
| 13 | 2026-08-19 | セッションを閉じるとき、`contenteditable` を外すだけでなく**選択レンジを消し・blur し・操作レイヤにフォーカスを移す** | キャレットと iframe のフォーカスが残ると、ホスト window に key イベントが届かなくなる | contenteditable を外すだけ | 反映済 |
| 14 | 2026-08-19 | 文字書式は **pointerdown(capture)の瞬間に Range を退避**し、コマンド直前に復元する | ネイティブの select やカラーダイアログはフォーカスを奪い、WebKit はその時点で iframe の選択範囲を失う | 確認ボタンを置いて選択を保つ(操作が増える) | 反映済 |
| 15 | 2026-08-19 | `focus()` が作る**人工的なキャレットは信用しない** | それを「生きた選択」と見なすと、退避の復元をスキップして書式が別の場所に当たる | 生きた選択として扱う | 反映済 |
| 16 | 2026-08-20 | 編集中の「編集中 · ✓ 完了」バッジを**外す** | 常時ちらつく割に、同じことがクリックと Esc でできる。枠の色を変えるだけで足りる | バッジを残す | 反映済 |
| 17 | 2026-08-20 | パンくずのホバー枠は選択とは別の **`focusUid`** に持つ | `uid` に混ぜると、インスペクタ・コマンド・履歴といった下流が**マウスの通り道を編集対象と見なす** | 選択 uid を一時的に書き換える | 反映済 |
| 18 | 2026-08-20 | パンくずに**スライドルートを載せない** | 各項目はクリックで選択できるので、載せると「スライドは選択できない」ルールを 1 クリックで迂回できてしまう | 載せる(ルールが穴だらけになる) | 反映済 |
| 19 | 2026-08-21 | **白紙スライドの追加を削除**し、新規は既存スライドからの派生(複製)だけにする | デッキのスライドは背景・フォント・ラッパの属性を共有しているので、それを持たない 1 枚は列の途中で別の文書に見える | 白紙を足せるようにする | 反映済 |
| 20 | 2026-08-20 | Undo は**選択も復元する**(`HtmlSnapshotCommand`) | 削除を Undo すると要素は戻るが選択が戻っておらず、オーバーレイが出ないので気付きにくかった。後続の smoke がまとめて落ちて初めて分かった | 要素だけ戻す | 反映済 |
| 21 | 2026-08-20 | 無いフォントは**グレーアウトではなく非表示**にする | 選べてしまうと、ブラウザが黙って別の書体に落として「別のマシンで開くと見た目が変わる」ことに気付けない | グレーアウトで見せる | 反映済 |
| 22 | 2026-08-20 | フォント実在判定は `document.fonts.check()` ではなく **canvas の幅比較(総称 3 つ)** | `fonts.check()` は `@font-face` の登録状況を答える API で、ローカルインストール済み書体については何も言わない。総称を 3 つ試すのは、macOS の `sans-serif` 自体が日本語ゴシックで、CJK だけで測るとヒラギノが「無い」と判定されるため | `document.fonts.check()` を使う／総称 1 つで測る | 反映済 |
| 23 | 2026-08-20 | 自動保存は**テンポラリ + 復旧フローを挟まず、書き出し先へ直接書く** | 書き込みの安全性は atomic rename で足りている。復旧フローは判断をユーザーに投げる UI が増えるだけ | テンポラリに書いて起動時に復旧を提案する | 反映済 |
| 24 | 2026-08-21 | 終了確認は OS ダイアログではなく**アプリ内モーダルの 3 択** | `plugin-dialog` の `ask` はボタンが 2 つしか置けず、**× の押し間違いから戻る道が無かった** | OS の ask ダイアログを使う | 反映済 |
| 25 | 2026-08-20 | 取り込んだ HTML のパスを**書き出し先として自動採用しない** | 元ファイルを黙って上書きしないため。場所を決めるのはユーザー | 取り込み元をそのまま保存先にする | 反映済 |
| 26 | 2026-08-21 | スライド外側のクリックによる選択解除は、キャンバスが**直接のイベントターゲット**かどうかで判定する(`event.target === event.currentTarget`) | `.stage-viewport` はスケール後のスライド矩形そのものなので、キャンバス自身に届いた = スライドの外、が厳密に成り立つ。テキスト編集の window capture リスナ(`closest()` の除外リスト方式)と違い、パネルやツールバーを列挙し続ける必要が無い | 除外リスト方式で window に張る／`getBoundingClientRect` と座標比較する | 反映済 |
| 27 | 2026-08-22 | **トリミング枠の中の画像はクリックで枠を選ぶ**。枠は「中身を持つもの」= 背景ではない、として扱う | 画像は枠より意図的に大きいので、画像を選ぶとトリミング前の矩形にハンドルが出て、ドラッグが枠内スライドになる。`isSelfContained` に枠を足さないと、全面写真の枠が背景扱いになって選べなくなる | 置換要素の遡上停止をそのままにして、インスペクタ側で読み替える(ハンドルとドラッグは直らない) | 反映済 |
| 28 | 2026-08-22 | ドラッグ中の **Esc は履歴に載せずに開始位置へ戻す** | 始めたドラッグから逃げる手段が Undo しか無いと、確定していないものを一度確定させることになる。`cancel()` は前から実装されていたが呼び出し元が無かった | 取り消しもコマンドとして積む(「移動して戻した」履歴が 2 段残る) | 反映済 |
| 29 | 2026-08-22 | **Esc の優先順位は EditStage の keydown 1 箇所で決める** | 意味が 4 つ(ドラッグ取り消し・トリミングのドラッグ取り消し・トリミング終了・選択を外へ)に増えた。分散させると 1 回の Esc で 2 つ発火する | 各コントローラが自分で window に張る | 反映済 |
| 30 | 2026-08-22 | `pointerdown` は**主ボタン以外を無視する** | 右クリックはメニューを求める操作。以前は右ドラッグで要素が動き、開いたメニューの下から対象が逃げていた | 右ボタンでも移動を始める(従来の挙動) | 反映済 |
| 31 | 2026-08-22 | リサイズの吸着は**掴んだ辺 1 本だけを候補に差し出す**(`snapToGuides` の `edges`) | 6 本すべてを出すと、東ハンドルを引いているのに左辺が吸着して箱ごと横滑りする。`edges` 省略時は 6 本のまま = 移動時の既存挙動と既存テストを保つ | リサイズ専用の吸着関数を別に書く(2 実装に分かれる) | 反映済 |
| 32 | 2026-08-22 | リサイズ吸着も**予測せず実測**する(#10 と同じ) | 辺の着地点を `start.cx` から計算すると flex 中央寄せの箱で必ず外れる。増える強制レイアウトは、他要素の bounds をジェスチャ開始時に 1 度だけ測ることで相殺 | 開始時の中心とサイズから辺の位置を計算する | 反映済 |
| 33 | 2026-08-22 | **回転済みの要素と縦横比固定中はリサイズ吸着しない** | 回した箱の軸平行 bounds の辺を吸わせても意味が合わず、比を保ったまま片側だけ動かすこともできない | 回転時も軸平行 bounds で吸着させる | 反映済 |
| 34 | 2026-08-22 | ドラッグ中の数値バッジの単位は**デザイン px** | インスペクタが見せている値・HTML が持っている値と同じ空間。スクリーン px はズームで意味が変わる | 実際の画面 px で出す | 反映済 |
| 35 | 2026-08-22 | ハンドルのカーソルは**基準角 + 回転角を 180° で丸めて**決める | リサイズカーソルは双方向なので 360° 周期ではなく 180° 周期。8 ハンドル × 任意角度が 4 種類に落ちる | ハンドルごとに回転域の表を持つ | 反映済 |
| 36 | 2026-08-22 | 重ね順とズームのショートカットは **`event.code` で判定する** | 括弧・マイナスは JIS と US で同じ物理キーが別の文字を出す。`event.key` で書くと片方の配列でしか効かない | `event.key` で `']'` などを見る | 反映済 |
| 37 | 2026-08-22 | Tab を横取りするのは**ステージにフォーカスがあるときだけ** | 常に奪うとツールバーとインスペクタのタブ順が消え、キーボードだけで操作できなくなる | window で常に preventDefault する | 反映済 |
| 38 | 2026-08-22 | Tab は**同じ親の兄弟だけ**を巡回する(スライド全体を舐めない) | クリックで 1 階層ずつ潜る操作と揃う。全要素を平坦に舐めると、入れ子の深い箱が脈絡なく現れる | スライド内の選択可能要素を DOM 順に全部巡る | 反映済 |
| 39 | 2026-08-22 | Tab はクリックが**選ばない兄弟を飛ばす**(`chooseSelectionTarget(el) === el`) | 飛ばさないと、ポインタでは到達できない背景扱いの箱がキーボードでだけ選べてしまう | 兄弟をそのまま順に選ぶ | 反映済 |
| 40 | 2026-08-22 | Alt+ドラッグは**コピーを置き去りにし、元の要素を動かす** | 逆にすると動いている側の uid が途中で入れ替わり、選択・開始時スナップショット・吸着の除外を全部張り替えることになる。画面上の結果は同じ | コピーのほうをポインタに追従させる | 反映済 |
| 41 | 2026-08-22 | Alt+ドラッグの Undo は **`HtmlSnapshotCommand`** | `StyleSnapshotCommand` は `style` 属性しか戻せず、ノードの増減を表現できないので Undo してもコピーが残る | style スナップショット / 複製と移動を 2 コマンドに分ける(1 undo にならない) | 反映済 |
| 42 | 2026-08-22 | markup を取るのは **Alt が押された瞬間だけ** | 全ドラッグの開始時に取ると、複製しないドラッグにもフルシリアライズのコストが乗る | ドラッグ開始時に必ず取る | 反映済 |
| 43 | 2026-08-22 | 「before」を取る前に**要素を開始位置へ巻き戻す** | 巻き戻さないと「少し動いた要素」が before に写り、Undo が開始位置に戻らない。この巻き戻しはコピーの位置決めも兼ねる | ドラッグ開始時の transform を別に覚えて後から補正する | 反映済 |
| 44 | 2026-08-22 | 複製は**ドラッグが動き出してから 1 度だけ** | Alt を押しただけでポインタが止まっていると、真上に重なったコピーができる | Alt の押下で即座に複製する | 反映済 |
| 45 | 2026-08-22 | `cloneInPlace` は**オフセットを足さない**(⌘D 側が足す) | ⌘D は「見えるところにずらす」、Alt+ドラッグは「開始位置に残す」で、要求が逆。共通部分は「同じ親の次の兄弟として写す」だけ | 複製関数がオフセットまで面倒を見る | 反映済 |
| 46 | 2026-08-22 | 浮遊 UI(コンテキストメニュー)は **`document.body` へ portal する** | `.stage-surface` の `transform: scale()` が `position: fixed` の包含ブロックを作る。ステージの中に描くとメニューがズーム倍率で拡大縮小し、座標も surface 原点基準になる | ステージの子として描く | 反映済 |
| 47 | 2026-08-22 | `Menu` と `ContextMenu` は**別コンポーネントにし、`MenuItem`・`.menu-popup`・`useDismiss` を共有する** | `Menu` は「ボタン + 親相対のドロップダウン」でトリガー込みの部品。統合すると `label` / `icon` / `disabled` が全部 optional の抜け殻になる。違うのは配置ロジックだけ | `Menu` を汎用化して 1 つにまとめる | 反映済 |
| 48 | 2026-08-22 | **テキスト編集中は右クリックメニューを出さない** | そのとき操作レイヤは `pointer-events: none` で、右クリックはスクリプト無効 iframe に落ちる。ホストから `preventDefault()` できないので、カスタムメニューは OS のメニューと二重に出る(#11 と同じ制約) | 出そうとする(WebKit で二重表示) | 反映済 |
| 49 | 2026-08-22 | 右クリックのハンドラは**操作レイヤとオーバーレイのルートの両方**に置く | ハンドルは `.stage-overlay` 側にあり操作レイヤの兄弟なので、片方だけではハンドル上の右クリックを取りこぼす | 操作レイヤだけに置く | 反映済 |
| 50 | 2026-08-22 | 「テキストを編集」は**ストア越しにステージへ依頼する**(`textEditRequest`) | `contenteditable` の出入りの規則はステージに集約されている。`features` から直接触ると規則が 2 箇所に分かれる。`useCropSession` と同じ形 | メニューから直接 `contenteditable` を立てる | 反映済 |
| 51 | 2026-08-22 | メニューの整列は**左右中央・上下中央の 2 つだけ**にする | 右クリックで欲しいのは「真ん中に置く」で、6 辺すべてはツールバーの役目。項目が増えるほど探す時間が延びる | 6 辺すべて出す | 反映済 |
| 52 | 2026-08-23 | **「自前のテキストを持つ」判定はインライン要素を透過する**(直下のテキストノードだけを見ない) | 文字書式は `execCommand` なので、当てた瞬間に中身が `<b>` / `<span>` に包まれる。透過しないと書式を当てた要素が「テキストを持たない」に落ち、ダブルクリック・F2・右クリックのすべてから**再入場できなくなる**(全幅の見出しは背景扱いになり選択もできない)。遡上ルールと同じ概念に揃える | 直下のテキストノードだけを見る(不具合の原因) / エディタ由来のマークアップに印を付けて区別する(書き出し HTML を汚すので ADR-0001 と衝突) | 反映済 |
| 53 | 2026-08-23 | テキストセッションの履歴は**基準マークアップ 1 つ**を書式適用とセッション終了で共有し、進めながら記録する | 書式は `withUndo` が、入力はセッション終了が記録する。終了側がセッション開始時のマークアップを基準にしていたため、書式を当てて閉じると**同じ変更が 2 件**積まれ、merge 窓(800ms)を過ぎていると Undo 1 回目が空振りした。セッション中の Undo で基準がずれる問題も同じ形なので、履歴ストアの購読で追従させる | 終了時に「直前のコマンドと同じか」を照合する(書式の後に入力が続くと基準がずれるので不成立) | 反映済 |
| 54 | 2026-08-23 | テキストセッションの差分を記録する直前に**必ず `reindex()` する** | `execCommand` が作ったノードには uid が無い。要素は uid で指す約束(ADR-0003)なので、uid の無い `<li>` はクリックから存在しないのと同じ — 箇条書きを当てた行が選択も再編集もできなくなっていた。`SetInnerHtmlCommand` は書き込み時に reindex するが、セッション内の変更は `alreadyApplied` で通すため誰も配らない。マークアップを読む前に配るので、履歴に載る文字列と DOM が一致する | 書式コマンドごとに個別に配る(入力が作るノードが漏れる) / クリック側で uid の無い要素に遅延で配る(触る側すべてに規則が散る) | 反映済 |
| 55 | 2026-08-23 | 箇条書きを当てたあとの**テキスト編集の単位は `<li>` 1 行**のままにする | クリックは常に最も内側のブロックを選ぶ、という既存規則の当然の帰結。デッキが元から持っている `<ul>` も今は行単位で編集するので、由来(作者が書いた / 書式で作った)によって同じ形の要素の挙動が変わるほうが説明できない。PowerPoint 式の「テキストボックス全体を 1 セッション」は遡上規則そのものの変更なので、[ideas.md](../../ideas.md) #9 に積む | 書式で作った `ul` のときだけ親へ戻す(由来で挙動が割れる) | **取り下げ(#56 で覆した)** |
| 56 | 2026-08-23 | **`<ul>` / `<ol>` / `<li>` を選択に対して透過**させ、リストを持つ要素そのものを選択・編集する(#55 を覆す) | #55 の「行単位で一貫させる」は、**解除できない**という致命傷を見落としていた。編集ホストが `<li>` だと `<ul>` はその外側にあり、`execCommand` はホスト内のリストしか外せないので、箇条書きは当てられるのに二度と外せない。透過させれば編集ホストがリストを持つ要素になり、適用と解除が同じボタンで対称になる。ついでに「箱が 2 つ増える」問題も消える | 編集のときだけ親へ昇格させる(選択枠と編集ホストがずれ、シールドの穴が `<li>` の大きさになる) | 反映済 |
| 57 | 2026-08-23 | リストコマンドが**新規に作り、かつ以前のマークアップに無い宣言を持つ** `<span>` を剥がす | 解除時にエンジンがリスト内の見た目(`font-size:26px`)をインラインで焼き付けるため、適用 → 解除で**見出しが 44px から 26px に縮み**、デッキの `h2` ルールが二度と届かなくなる(ADR-0004 違反)。「新規」だけでは足りない — 適用時にユーザー指定の span がエンジンに組み直されて新品として現れるので、**以前に無かった宣言**という条件と併せて初めて residue と区別できる。class 等の属性を持つ span はデッキのものなので触らない | 親と computed 値が同じ span を剥がす(焼き付けた 26px は親の 44px と違うので残ってしまう) / 常に剥がす(ユーザー指定を失う) | 反映済 |
| 58 | 2026-08-23 | 書式ボタンの適用中状態を**200ms のポーリング**で表示する | `queryFormatState()` は実装済みだったが**どこからも呼ばれておらず**、B は常に消灯だった。デッキの見出しは元から太字なので、押すと「当たらない」ではなく**デッキの太さが外れる** — トグルであることが見えないのが原因。ステージはスクリプト無効で `selectionchange` がホストに届かないため、イベントでは取れない(ADR-0002)。4 回の `queryCommandState` をセッション中だけ回す | frame の document に `selectionchange` を張る(WebKit で発火しない) / 押下時だけ引き直す(キャレット移動に追従しない) | 反映済 |
| 59 | 2026-08-23 | **ダブルクリックの意味は「テキスト編集 / トリミングを始める」**(設計書が書いていた「1 階層内側へ」を取り下げ、実装を正とする) | 設計書と実装が食い違っていた([issues.md](../../issues.md) #3)ので裁定を仰いだ結果。PowerPoint はダブルクリックでテキスト編集に入るので、実装のほうが「独自の操作体系を発明しない」というこの製品の判断軸に合う。設計書自身も「テキスト編集の入口はダブルクリック・Enter / F2・右クリックの 3 つ」と別の行で書いており、内部矛盾していた。階層を降りる手段はパンくずと Tab(#38)が既にある | 設計書を正として実装を変える(テキスト編集の入口を 1 つ潰すことになり、代わりの入口を発明する羽目になる) | 反映済 |
| 60 | 2026-08-23 | 貼り付けの洗浄は **`paste` リスナではなく MutationObserver**(挿さったあとに洗う) | 編集フレームはスクリプト無効で、WebKit はその document のリスナを発火させない(#2 と同じ壁)。Chrome だけ通って実アプリで壊れる実装になる。Mutation records は境界を越えるので、テキストセッションのオーバーレイ追従に使っている観測に相乗りできる | フレームに `paste` を張る(WKWebView で発火しない) / 保存時にまとめて洗う(デッキ自身のマークアップと区別が付かない) | 反映済 |
| 61 | 2026-08-23 | 貼り付けで残すのは**強調だけ**(`font-weight` / `font-style` / `text-decoration*`)で、サイズ・書体・色は落とす | 太字・斜体はユーザーが選んだ意味だが、サイズや色は**貼り元の文書のデザイン**。持ち込むとデッキのデザインと衝突する(実測で `font-size:99px` が書き出しに混入)。このアプリの価値は「他人のデッキを壊さない」ことなので、迷ったらデッキの CSS に勝たせない側へ倒す | `style` を丸ごと残す(貼り元のデザインがデッキを上書きする) / `style` を丸ごと落とす(span で太字を表現するアプリからの貼り付けで強調が消える) | 反映済 |
| 62 | 2026-08-23 | 洗浄では**要素を unwrap しない**。属性を落として裸になった `<span>` はそのまま残す | `<div>` を剥がすと貼り付けた中身のレイアウトが変わる。「解釈できないものは触らない」の原則(rules §0-1)に倒す。裸の span は見た目も往復回数も変えないので不変条件 2 を満たす | 属性の無い span を剥がす(出力は綺麗になるが、剥がしてよい範囲の線引きが要る) | 反映済 |
| 63 | 2026-08-23 | 自前の書式コマンド中はフラグで洗浄を止め、**フラグを下ろすのは `mutate()` の後に積んだマイクロタスク** | 観測側の通知は `mutate()` の最中に積まれる。マイクロタスクは FIFO なので、下ろす処理を先に積むと観測より前に走り、エディタ自身が作った `<b>` と span が洗われる。実際にそう書いて smoke が 3 件落ちた | `setTimeout` で下ろす(タスク境界まで開きっぱなしになり、直後の本物の貼り付けを取り逃す) / 洗浄側でエディタ由来のノードに印を付ける(書き出し HTML を汚すので ADR-0001 と衝突) | 反映済 |
| 64 | 2026-08-23 | **`pointercancel` は `cancel()`(巻き戻し)へ繋ぐ**。`pointerup` と同じ `end()` にしない | キャンセルされたジェスチャを確定すると、そこまで動いた数 px が残る。Windows の「ピクッと動いて止まる」([issues](../../issues.md) #16)は原因が何であれこの形で表に出るので、被害だけでも先に止める。`cancel()` は Esc 用に既にあったものをそのまま使う | ①`end()` のまま原因(a)〜(d)の特定を待つ ②cancel を無視してジェスチャを続行する(キャプチャが無い状態で座標を追えない) | 反映済 |
| 65 | 2026-08-23 | ステージのレイヤで**選択を始めさせない**(`preventDefault()` + `user-select: none` + `touch-action: none`)。セッション離脱時は**ホスト側の選択も畳む** | 選択済み範囲の上での press がネイティブドラッグに化けるのが #16 の候補 (a) と #17 の本体。実測でホスト側の選択が Esc 後も `isCollapsed: false` で残っていた。`.pane-divider` が #8 で同じ処方で直っている | CSS だけ / `preventDefault()` だけ(片方でも足りるが、スタイルシート紛失とハンドラ不発で壊れ方が違う) | 反映済 |
| 66 | 2026-08-23 | **編集中要素の上もホスト側の 1 枚で覆い**(`.overlay-text-shield`)、選択は `caretRangeFromPoint` + Selection API で**ホストが引く** | 選択済み範囲の上での press を、ブラウザは「新しい選択」ではなく**その範囲のネイティブドラッグ**と解釈する([issues](../../issues.md) #17)。スクリプト無効の document では `dragstart` を潰すリスナが動かないので、起きてから止めることはできない。press をフレームに渡さなければ起きようがない。#11 / #12 / #58 と同じ「観測はできないが、させることはできる」の一手 | ①注入 CSS の `-webkit-user-drag: none`(**効かない** — Blink / WebKit とも「押した点が選択の中にあるか」でドラッグ種別を先に Selection と決めるので、`-webkit-user-drag` は要素ドラッグの判定にしか効かない。`issues.md` #17 が想定していた筋だが誤り) ②フレームに `dragstart` リスナ(WKWebView で発火しない) ③許容(「クリックしてから引き直す」を覚えてもらう) | 反映済 |
| 67 | 2026-08-23 | #66 の代償として、**選択した文字をドラッグして移動する**(ブラウザ標準の text drag-and-drop)を捨てる | 「選択し直せない」は毎回起きるが、ドラッグ移動は切り取り + 貼り付けで代替できる。ホスト側で drag-and-drop 相当まで書くのは #66 の何倍もの作業で、ドロップ位置の判定・自動スクロール・取り消しまで全部自前になる | ドラッグ移動を残す前提で別の解を探す(ホスト側で drag-and-drop を自前実装することになる) | 反映済 |
| 68 | 2026-08-23 | 連打(単語 / 要素全体)の回数は**ホストが 400ms / 4px の窓で数える** | `PointerEvent.detail` は仕様上 0 で、イベントから読めない。`dblclick` に乗ると 3 連打が取れず、React の合成イベントと実イベントで経路が分かれる | `event.detail` を読む(Chromium でだけ動いたように見えて WebKit で崩れる形) / `dblclick` を併用する | 反映済 |
| 69 | 2026-08-23 | **ドラッグ開始のデッドゾーンは「画面上 4px」**で測り、ジェスチャ開始時のズームで `4 / scale` ステージ px に換算する。越えたあとは**始点基準**で追従する | 手の震えは物理的な距離なので、しきい値も画面で測らないと意味がズームで変わる。ステージ px に固定すると 46% 表示では画面上 1.8px しか吸収できず、[issues](../../issues.md) #18 がほぼ残る(実測で 3 画面 px = スライド実寸 9.83px)。判定は既存の `moved` フラグの立て方を差し替えるだけで、「履歴に積むか」と「Alt 複製を起こすか」の両方に自動的に効く | ①時間の遅延方式(トラックパッドで体感が悪い) ②ステージ px 固定の 4px(縮小表示で無力) ③しきい値を越えた地点を新しい原点にする(以降ずっと要素がポインタより 4px 遅れる) ④回転・リサイズにも同時に入れる(リサイズは `beginResize` の焼き付けの遅延も要り #24 の領域に入るので分けた) | 反映済 |
| 70 | 2026-08-23 | **デッドゾーンは移動・リサイズ・回転で共通**にし、判定を `move()` の入口 1 箇所に置く(`moved` / `deadZone` を 3 つの gesture が共通で持つ) | ハンドルにも同型の症状があった([issues](../../issues.md) #29) — 回転は掴んで離すだけで角度が変わり履歴が 1 段積まれ、リサイズはポインタが動かなくてもサイズが確定していた。分岐ごとに書くと `end()` の「積むか」判定も 3 通りになる。1 箇所に寄せると `if (!gesture.moved) return` 1 行で済む | ジェスチャごとに個別のしきい値判定を書く(同じ規則が 3 箇所に散る) / 移動だけに入れたままにする(ハンドル側の症状が残る) | 反映済 |
| 71 | 2026-08-23 | リサイズの**寸法ピン留めを `beginResize` から `#resize` の初回へ移す** | `pointerdown` の時点で、しかも before スナップショットより**前**に `width` / `height` を書いていたため、Undo が戻す先が「ピン留め済み」になり**リサイズを取り消しても寸法が残った**([issues](../../issues.md) #24)。元は `style` を持たない箱が固定サイズになり、書き出した HTML にも残る。移す先を「しきい値を越えた最初の 1 回」にすると #24 と #29 が同時に解ける — スナップショットは取り終えており、クリックで終わった press はそこへ到達しない | ①ピン留めを `captureStyles` の後ろへ動かすだけ(#24 は直るが、ハンドルのクリックで寸法が焼き付くのは残る) ②ピン留めを消す(`resizeKeepingAnchor` が毎フレーム寸法を書くので冗長に見えるが、内容で伸びる箱の初回 reflow を実機で確かめずに消せない) | 反映済 |
| 72 | 2026-08-24 | **編集開始のダブルクリックはキャレットを置くだけ**にし、単語を取るのは**セッション中の 2 連打**だけにする | 開始と同時に語が選択されていると、次の 1 打で**気づかないうちに語が消える**(実測: `Claude Code基礎講座` をダブルクリックして 1 文字打つと `Claude Code基礎ABC`)。⌘Z 1 回で戻せるが、「編集を始めた」つもりの操作が破壊的なのは割に合わない。単語選択は #66〜#68 で入った `TextSelectionController` が既に持っているので、**失う機能は無い** | ①現状維持(テキストを丸ごと打ち直す使い方には速いが、上の事故が残る。[issues](../../issues.md) #25 は「PowerPoint は編集開始のダブルクリックでキャレットしか置かない」を根拠に挙げていたが、**その前提には疑いがある**ことをユーザーに報告したうえでの裁定なので、採否の理由にはしていない) ②Enter / F2 と右クリックメニューも同時に揃える(PowerPoint の F2 は「全選択」で、末尾キャレットとも語選択とも違う。別の設計判断が要るので分けた) | 反映済 |
| 73 | 2026-08-24 | リストを建てた直後に**要素の行揃えを新しい `ul` / `li` へ配り直す**。**コマンドは積まず** `withUndo` の中で DOM に書く | `execCommand` が作る `ul` は行揃えを持たず、外側からの継承は `ul` 自身の padding・`outside` の行頭記号・デッキの `ul` / `li` 宣言に負ける([issues](../../issues.md) #27)。配り直さないと**中央揃え → 箇条書き**の順だけが崩れ、症状が半分残る。`withUndo` の中なら差分が `commitTextSession()` の `innerHTML` に含まれるので、リスト化は 1 手のまま | ①`SetInlineStyleGroupCommand` を続けて積む(1 クリックが履歴に 2 手乗る) ②リスト化の直後にユーザーがもう一度行揃えを押す前提にする(順序で結果が変わるのは操作体系の欠陥) ③配らない(#27 の半分が残る。裁定で両方向と決めた) | 反映済 |
| 74 | 2026-08-24 | **挿入するテキストボックスは空**にし、プレースホルダは編集ステージが `::before` で描く | 実データとして入れていた `テキストを入力` は、**エディタが選んだ言葉をユーザーの文書に置く**行為で、消し忘れればそのまま書き出しに残った。擬似要素の `content` は DOM に無いので、シリアライズ・複製・書き出しのどれにも入りようがない(不変条件 2)。`contenteditable` が付くのは編集中の要素だけなので、デッキ側の空要素は巻き込まない | ①既定文字のまま、挿入直後に全選択して打てば置き換わるようにする(打たずに抜ければ残るのは同じ) ②保存時に「エディタ既定の文字と同じなら消す」(ユーザーが本当にその文字を打った場合まで消える) ③`data-hse-*` の印を頼りに CSS で隠す(印はシリアライズで剥がれるので、開き直すと文字が戻る) | 反映済(仕掛けは #83 で組み直し) |
| 75 | 2026-08-24 | 一文字も入らずに終わったテキストボックスの挿入は **`revoke()` で履歴から取り下げる**。対象は**挿入した直後の 1 件だけ** | 空の箱は見えないので、残すと「選択もできない透明な箱」がスライドに溜まる。Undo で消すと**頼んでいない箱が redo に残り**、消すのにもう一手かかる。取り消せるのは最新の 1 手だけなので、間に別のコマンドが挟まったときだけ削除を 1 コマンドとして積む。既存の文章を全部消した場合に消さないのは、それがユーザーの要素だから(rules §0-1) | ①削除コマンドを積む(Undo が 2 回必要で、1 回目は見えない箱が選択枠付きで戻る) ②空の箱をそのまま残す(`isTextEditable` は「自前のテキストを持つ」要素しか通さないので、残した箱は**二度と編集に入れない**。緩めるとデッキ由来の空 div まで編集対象になり、背景判定(#52)に波及する) ③空になった要素は由来を問わず消す(デッキの要素をエディタが勝手に消すことになる) | 反映済(仕掛けは #83 で組み直し) |
| 76 | 2026-08-24 | `StyleSnapshotCommand` の畳み込みは **merge key を渡したときだけ**(既定は畳まない)。キーは `geometry:<欄>:<uid>`、畳んでも `before` は最初のものを残す | 同じコマンドをジェスチャ(移動・リサイズ・回転・トリミング)と**打鍵ごとに当たる数値欄**の両方が使っている。ジェスチャは `end()` の時点で 1 コマンドとして到着するので畳む必要が無く、無条件に畳むと**少し間を空けた 2 回のドラッグが 1 手になる**(併合窓は 800ms あり、ドラッグの間隔はそれより短くなりうる)。キーに欄の名前を入れるのは、X / Y / 回転が同じ `transform` を書くため — `SetInlineStyleCommand` のようにプロパティ集合では区別できない。`before` を最初のものに固定するのは、そうしないと Undo が 1 打鍵前にしか戻らず [issues](../../issues.md) #24 と同じ焼き付きが残るため | ①ラベルで畳む(`t('command.setGeometry')` は移動ジェスチャと別ラベルなので一見足りるが、ジェスチャ同士が畳まれる問題がそのまま残る) ②`SetInlineStyleCommand` のようにプロパティ集合で畳む(スナップショットは `style` 属性まるごとなので、そもそも集合を持たない) ③欄ごとに専用のコマンドクラスを足す(`alreadyApplied` の経路が 1 つ増え、[ADR-0003](../../adr/0003-all-edits-as-commands.md) の継ぎ目が 2 本になる) | 反映済 |
| 77 | 2026-08-25 | **`resyncTextBaseline` は Undo / Redo / revoke のときだけ効かせる**。判定は `useHistory` の `lastAt`(`execute()` が時刻を書き、`undo` / `redo` / `revoke` が 0 に戻す) | `EditStage` は履歴ストアそのものを購読しているので `execute()` の push でも発火していた。push で基準を進めると**そこまでの入力が「その入力に触っていないコマンド」に渡り**、どちらのステップにも属さなくなる。文字を打ってから行揃えを押すと履歴に 1 手しか残らず、戻しても打った文字が戻らなかった([issues](../../issues.md) #44)。`lastAt` は**ストアが既に引いている線**なので、同期させ続ける 2 つ目のフィールドも、呼び出し側が渡し忘れられるフラグも要らない | ①購読側にフラグを持たせる(`undo`/`redo` の入口で立てて落とす。状態が 2 つになる) ②`subscribeWithSelector` を入れる(zustand 5 の既定の `subscribe` は 1 引数版で、ミドルウェアの導入が要る) ③呼び出し側の規律だけで済ませる(`execute()` を足す人が毎回覚えている必要がある) | 反映済 |
| 78 | 2026-08-25 | **プレースホルダの「空か」判定を CSS から降ろす**。`isBlank()` で判定し、結果を `data-hse-blank` として要素に載せ、CSS はその印だけを見る | CSS にテキストノードを見る手段が無く、セレクタで書ける範囲に正解が無い。`:empty` は「全部消した後に残る `<br>` 1 つ」を空と見なさず、それを補った `:has(> br:only-child)` は `:only-child` が**子要素しか数えない**ため、`<br>` 1 つで改行しただけの要素すべてを空と判定した（タグ非依存。[issues](../../issues.md) #46）。`isBlank()` は「編集セッションに入れるか」「使われなかった箱を取り消すか」が既に使っている基準なので、**空の定義がプロジェクトに 1 つ**に揃う。印は `data-hse-` 接頭辞で、外し忘れてもシリアライズが剥がす（`bridge.ts` の `cleanElement`） | ①`:has()` の条件を細かくする（`<br>` の数や位置をいくら足しても、**テキストの有無だけは見られない**ので必ず穴が残る） ②`:empty` だけに戻す（「全部消した箱」でプレースホルダが出なくなり、#34 の決着が半分戻る） ③プレースホルダをオーバーレイ側に描く（DOM に一切触らずに済むが、行揃え・書体・行高を実要素と合わせ続ける必要があり、キャレット位置とずれる） | 反映済 |
| 79 | 2026-08-25 | リストの上書き(`padding-left` / 上下マージン / 高さ)を書くのは、**差分で取った「今この 1 押しが建てたリスト」だけ** | リスト化した瞬間に、箱から出ていく道が 2 つあった(挿入する 520×90 の箱・`font-size:28px` で Chromium 実測)。下へは UA スタイルの `ul { margin-block: 1em }` が 28px×2 を足し、リセットを持たないデッキで **64px はみ出す**。左へは `list-style-position: outside` の行頭記号がリスト自身の `padding-inline-start` の中に描かれるので、`* { margin:0; padding:0 }` のリセットを持つデッキでは**記号が負の x に落ちる**([issues.md](../../issues.md) #65)。差分で絞れば、同じ箱にデッキ作者が書いたリストがあっても触らない(rules §0-1)。行揃えの配り直し(#73)の**後**に走らせる — 左揃えの枝が `padding-left` を空にするので、先に書くと消える | ①注入 CSS で `ul` / `ol` に当てる(編集ステージの見た目だけが直り、書き出した HTML では変わらずはみ出す) ②箱の中のリスト全部に書く(デッキ作者のリストの見た目を変える) ③デッキ側のリセットの有無で分岐する(スライドごとに条件を数え上げることになり、実測より当たらない) | 反映済 |
| 80 | 2026-08-25 | 空の要素で編集を始めるとき、**`<br>` を 1 つ貸す**(印付き。最初の打鍵でブラウザ自身が消し、一文字も打たれなければ終了時に回収する) | 空の要素は**行ボックスを持たない**ので、キャレットに立つ場所が無い([issues.md](../../issues.md) #66)。Chromium 実測(本番と同じ sandbox・同じ注入 CSS)では `document.hasFocus()` も `activeElement` も `(div, 0)` の collapsed range も**すべて正しいのに、点滅するものが何も描かれない**。`<br>` を 1 つ入れると出た。回収するのは**まだ空のあいだだけ**なので、打った人の改行は消さない。書き出しに漏れないのは印が足場(#84)だから | ①プレースホルダを通常フローから外すだけにする(実測で出ない) ②`min-height` や `line-height` で箱に高さを作る(行ボックスの有無とは別の話で、実測でも出ない) ③空でも入場させず、先に一文字入れておく(#74 が捨てた「エディタが選んだ言葉を置く」に戻る) | 反映済 |
| 81 | 2026-08-25 | プレースホルダの `::before` を **`position: absolute` でフローから外す**(オフセットは `auto` のまま)。要素側に `position: relative` は**当てない** | フロー内では行頭の実インラインボックスになるので、キャレットが `テキストを入力\|` と**文字列の末尾に回っていた**。オフセットを `auto` に保てば static position のままなので、余白も `text-align` もそのまま効く。`relative` を当てないのは、このセレクタが**セッション中に中身を消した要素にも当たる**ため — デッキが絶対配置している見出しが、最後の 1 文字を消した瞬間に飛ぶ。空のあいだだけ `caret-color` をエディタ色にするのも同じ組(挿入する箱は `color:#14161a` を決め打ちで持つので、暗いデッキではキャレットが溶けていた) | ①要素側に `relative` を当てて `inset` で位置を決める(デッキの要素の座標系をエディタが書き換えることになる) ②プレースホルダをオーバーレイ側に描く(#78 の却下案③と同じ — 書体・行高・行揃えを実要素と合わせ続ける必要があり、キャレット位置とずれる) | 反映済 |
| 82 | 2026-08-25 | セッションの**出口**で、**箱直下の単独ラッパーだけ**を畳む(子ノードがちょうど 1 つ / それが `div` / `data-hse-uid` 以外の属性を持たない、の 3 つ全部) | `contenteditable` の中で Enter を押すとブラウザは新しい行を `<div>` で包み、実測すると **1 行目だけ裸のテキストノード・2 行目以降が包まれる**非対称になって、`<div uid><div>あ</div></div>` と箱の下に要らない階層が 1 枚増える([issues.md](../../issues.md) #67)。`<p>` の中で押すと再パースで `<p>` が割れて空の `<p>` が増える(不変条件 2 ② の破れ)。複数行の `div` は**改行の意味を持つ**ので触らない。貼り付けの洗浄が unwrap しない(#62)のとは別の話で、あちらは他所から来たマークアップ、こちらはブラウザが勝手に足した包み。置く場所が `commitTextSession` の中ではないのは、`withUndo` が書式コマンド 1 回につき `commitTextSession` を 2 回呼ぶため — セッション中に走ると `<div><br></div>` が畳まれて**キャレットの container ごと消える** | ①`defaultParagraphSeparator` で包まないようにする(**選べるのは `div` か `p` かだけ**で、包まない選択肢は無い) ②Enter を横取りして自前で行を作る(編集フレームはスクリプト無効でキーがホストに届かない / [ADR-0002](../../adr/0002-edit-preview-separation.md)) ③`div` を由来を問わず畳む(貼り付けや デッキ由来のレイアウトが動く) | 反映済 |
| 83 | 2026-08-25 | **挿入したテキストボックスは、編集セッションを開かずまず選択状態で渡す**(PowerPoint と同じ二段階。#74 / #75 が組んだ仕掛けを 4 点セットで組み直す) | 編集モードに入ると操作レイヤが `pointer-events: none` になり Overlay もハンドルを描かないので、**挿入直後の箱は移動もリサイズもできなかった**([issues.md](../../issues.md) #68)。#74 / #75 は「挿入 = セッションが開いている」を前提に 3 つの仕掛けを組んでいたので、同時に 4 つ入れないと成立しない —— ①挿入は選択のみ(`insertTextBox` から request を落とす) ②空箱への再入場を**挿入直後の 1 件だけ**許す(`isTextEditable` 本体は触らず、呼び出し側で `isUntouchedTextBox` を `\|\|` する) ③プレースホルダを選択中にも描く(CSS セレクタから `contenteditable` を外し、ガードを script 側 = pending の 1 件にだけ印を付ける、へ降ろす。#78 が「空か」を CSS から降ろしたのと同じ考え方) ④破棄の合図を「セッション終了」から「選択が離れた / スライドが変わった / モードが変わった / デッキの形が変わる」へ移す。ダブルクリックで入って一文字も打たずに Esc したときは、**箱を残して選択状態に戻す**(pending は保つ — 消すと空箱への再入場が 1 回しか使えない) | ①`isTextEditable` 自体を緩めて空要素を通す(#75 の却下案②と同じ理由で、デッキ由来の空 div まで編集対象になり背景判定 #52 に波及する) ②編集セッション中もハンドルを描く(操作レイヤとテキスト選択のどちらが press を取るかが要素ごとに割れ、#66 のシールドが崩れる) ③挿入直後だけハンドルを出す特例を Overlay に置く(「編集中はハンドルを描かない」の規則が要素の由来で割れる) | 反映済 |
| 84 | 2026-08-25 | 編集ステージが足す**足場の正を `bridge.ts` の `SCAFFOLD_ATTRIBUTES` 1 箇所**に集め、markup を写す door すべてがそこを見て落とす。`data-hse-uid` だけは**door ごとに扱いが逆**なので集合には入れない(履歴では残す・複製では落とす) | #83 でプレースホルダが「印が付いているか」だけで描かれるようになった以上、印を載せたまま markup を写すと**消えないプレースホルダが焼き付く**(実測: `slideMarkup`(履歴)と `stripEditorMarks`(複製)が落としていなかったため、⌘D と ⌘Z で再現)。足場が preview 限定の合成と同じ性質(不変条件 8)を持つ以上、door が増えるたびに同じ知識を写していては必ず取りこぼす | ①door ごとに落とす属性を列挙する(door が増えるたびに同じ知識を書き写す) ②印をやめて DOM の状態から描く(#78 で CSS から降ろした判定に戻ることになる) ③書き出しの直前にまとめて落とす(履歴と複製は書き出しを通らない) | 反映済 |
| 85 | 2026-08-25 | **復元中(Undo / Redo / revoke)に届いた `revoke()` は `false` を返して何もしない**(engine の `restoring`)。`undo` の publish → revert の**順序は変えない** | スナップショットの復元は**選択を動かす**(削除の Undo は要素を選択状態で返す)。ステージはその選択を見て「挿入した箱が見捨てられたか」を判断するので、挿入の Undo が revert → 選択が動く → 箱は要らないと判断 → `revoke` と、`undo` の最中に engine へ再入場していた。しかもスタックの publish は revert の**後**なので `revoke` は同じコマンドを二度 revert し、2 回目が **post-undo の選択**を戻り先として焼き付ける(実測: 挿入 → ⌘Z → ⌘⇧Z で箱が非選択で戻り、pending も消えているので**見えも入れもしない**)。publish を先にするほうが真の直しだが、**すべてのコマンドの `apply` / `revert` は「履歴が今どこにいるか」を映すスタックの上で走る**のが engine の契約で、1 人の呼び出し側のためにそれを動かせない | ①`undo` で publish を revert より先にする(全コマンドが依存する契約を、呼び出し側 1 つのために変える) ②ステージ側で「今の選択移動は履歴由来か」をフラグで持つ(#77 の却下案①と同じ形で、同期させ続ける状態が 2 つになる) | 反映済 |
| 86 | 2026-08-25 | 編集を始めるフォーカスは **iframe 要素 → フレームの `window` → 要素そのもの**の 3 段で、外から内へ渡す(`holdTextFocus`)。先頭で `offsetHeight` を読んでスタイル再計算を流す | キャレットは「**フォーカスされたフレームの、フォーカスされた要素**」にしか描かれない。セッションを開くきっかけの press は `handleLayerPointerDown` が**ホスト側の `.stage-interaction` にフォーカスを載せている**(ダブルクリックなら 2 回)のに、`startTextEditing` は `element.focus()` しか呼んでいなかった([issues.md](../../issues.md) #69)。Chromium は中の要素を focus した副作用でホスト文書のフォーカスも iframe へ動かすので 1 行で足りていたが、**それはエンジンの親切**で WebKit は面倒を見ない。順序は逆にできない(前 2 つはフレーム側の `activeElement` を `body` に戻す)。直前に `contenteditable` を付けた要素は、再計算が済むまで focusable と見なされないことがある | ①`element.focus()` だけ(Chromium でだけ動く形) ②`caretRangeFromPoint` の答えをそのまま張る(箱の padding や行間に当たった点では**隣や親の range が返り**、`contenteditable` でない場所にキャレットが立って打鍵も落ちる。`element.contains()` を通らない点は捨てて末尾に落とす) ③`removeAllRanges` + `addRange`(フレームの選択が一瞬 0 本になる状態を経由する。`selection.collapse` に置き換えた) | 反映済 |
| 87 | 2026-08-25 | Enter が残す**末尾の空ブロックを、セッション終了時に落とす**。落とすのは**末尾だけ・中身が読めないものだけ・属性を持たないものだけ**(`data-hse-uid` は属性に数えない)。単独ラッパーを畳む(#82)**前**に走らせる | Enter は `contenteditable` では確定ではなく `insertParagraph` を走らせる編集コマンドで、ブロックを 1 つ生む。実デッキで `aaa` → Enter は `aaa<div><br></div>` を残し、**子が 2 つあるので #82 の対象外**。しかもその空 div は `reindex()` で uid を貰うので独立して選択でき、**箱の中に選択枠が 2 つ出る**([issues.md](../../issues.md) #70)。段落のあいだの空行はユーザーが空けた間隔なので残す。属性を持つブロックも残す(`justifyCenter` が単一行の箱に置く `div style="text-align:center"` を剥がすと、揃えた行が動く)。uid を属性に数えないのは `reindex` が全要素に押すため。#82 より前なのは、末尾を落とした結果が単独ラッパーになることがあるから(`<div>aaa</div><div><br></div>` → `<div>aaa</div>` → `aaa`) | ①Enter を横取りして自前で段落を作る(#82 と同じ壁 — WKWebView でキーが届かない / [ADR-0002](../../adr/0002-edit-preview-separation.md)) ②空ブロックを位置に関係なく全部落とす(ユーザーが空けた間隔が消える) ③`reindex()` の側で空ブロックに uid を配らない(選択枠は消えるが、uid の無い要素が残るのは [ADR-0003](../../adr/0003-all-edits-as-commands.md) の前提を崩す) | 反映済 |
| 88 | 2026-08-25 | 行頭記号のために空ける余白は、**`list-style-position` を一時的に `inside` へ倒して文字の開始位置の差を測る**(`measureMarkerAdvance`)。確保するのは**実測値 + 0.3em**、測れなかったときだけ 1.2em | 最初は箇条書きの慣習値 1.2em を決め打ちで書いていたが、**番号付きの行頭記号はずっと幅を食う**。ユーザーのデッキで実測すると 76px の見出しを `1.` にするとマーカーだけで 89.02px を要求し、1.2em(91.2px)は余裕が **2.18px** — 枠線に接して見えるうえ、`10.` に届けば確実に外へ出る([issues.md](../../issues.md) #71)。マーカーの幅は DOM から訊けない(`::marker` は箱を渡さず、`getComputedStyle(li, '::marker')` が答えるのは種別と色だけ)。だが `outside` は行から外へ吊るし `inside` は行の中に置くので、**その差がマーカーの送り幅そのもの**。書体・字数・カウンタ種別に依らず答えが出る。+0.3em は文字に接して見えないための余白で、`10.` の 2 桁目の分でもある。em で書くのは、あとで文字サイズを変えてもマーカーと一緒に動かすため。実デッキ(h1 76px)で 1.2em = 余裕 2.18px → 1.48em = 余裕 23.46px | ①1.2em の決め打ちを続ける(番号付きで外に出る) ②カウンタ種別と桁数から幅を計算する(書体ごとの字幅を持つことになり、デッキの書体では当たらない) ③`inside` に倒したままにする(行頭記号が文字と同じ行に入り、折り返した 2 行目の頭が揃わない) | 反映済 |
| 89 | 2026-08-25 | 挿入するテキストボックスは **`height` を書かず `min-height` を 1 行分だけ**持つ(28px × 1.5 = 42px)。既定サイズの正は `shapes.ts` の `TEXT_BOX_SIZE` 1 箇所 | `height:90px` を決め打ちで持っていたので、Enter で何行入れても 90px のままだった([issues.md](../../issues.md) #72)。`min-height` ならブラウザが中身に合わせて高さを決めるので、**何も観測しなくていい**のがこの形の要点 — ステージのフレームはスクリプトを走らせないので打鍵はホストから見えない([ADR-0002](../../adr/0002-edit-preview-separation.md))。床が要るのは空の箱を掴めるようにするため(高さを一切書かないと空の箱は 0px になる。上に描くプレースホルダは #81 でフローの外にあり、箱の高さに寄与しない)。図形(四角形・楕円など)は今までどおり `height` を持つ — 中身を持たないので自分では高さを決められない。実測: 挿入直後 42px、Enter で 42 → 84 → 126px、ホストが描く選択枠も 30 → 59 → 89(71% 表示)と追従 | ①打鍵を観測して高さを書き直す(フレームはスクリプト無効で打鍵が見えない) ②`height:auto` を書く(絶対配置の箱では 0px になり、空の箱が掴めない) ③テキストボックスにも図形と同じ既定高さを持たせたまま、はみ出したときだけ伸ばす(伸びはするが縮まないので、行を消しても箱が大きいまま残る) | 反映済 |
| 90 | 2026-08-25 | 貼り付けの洗浄が残す / 落とすの線引きを、**「箱の中身が既に宣言している値と、値まで一致する宣言は残す」**にする(#61 の「強調だけ残す」に足す) | 記録は「何が現れたか」しか言わず「なぜ現れたか」は言わない。**ノードを増やすのは貼り付けだけではなく、Enter がいちばん増やす** — Chrome は行を割るときにキャレットのいる run を丸ごと複製するので、48px の行末で改行すると `<div><span style="font-size:48px"><br></span></div>` になる。それを貼り付けとして洗うと 0.2 秒後にサイズが剥ぎ取られ、次に打った文字は箱の既定に戻る。これが「フォントサイズが追従しない」の中身だった([issues.md](../../issues.md) #78)。値で引けば、ブラウザが編集中に作るものは**必ず箱の中にある run の複製**なので常に通り、Word の `font-size:99px` は通らない。属性は一切緩めていない(Chrome は run を `data-hse-uid` ごと複製するので、割れた行の両方が同じ uid を名乗る) | 実ブラウザで測って全部落ちた素朴な規則が 3 つ —— ①「エディタのコマンド中だけ洗浄をスキップ」(#63 の延長。こちらが作るノードしか覆わず、ブラウザが勝手に作る複製は覆えない) ②「祖先が持つ宣言なら残す」(48px は**前の行の兄弟**にあるので効かない) ③「裸の span はブラウザ製」(Google ドキュメントが裸の span を貼るので逆に効かない) | 反映済 |
| 91 | 2026-08-25 | 編集中の ⌘Z は **フレーム document の `keydown` で横取りして `preventDefault`** し、アプリの undo / redo に回す。**保険**として、変異が落ち着いた rAF で uid を持たない要素があれば `reindex` を回す | 放っておくとネイティブ undo が勝ち、アプリの履歴は動かないまま DOM だけが戻って、**戻った markup は uid を失う** — 要素は uid で指す約束([ADR-0003](../../adr/0003-all-edits-as-commands.md))なので、それは「クリックから存在しない要素」になる。横取りが効くのは Chromium だけ(スクリプト無効の document で WebKit はホストのリスナを発火させない / [ADR-0002](../../adr/0002-edit-preview-separation.md))。**2 段構えにしたのは勝てないエンジンがあると分かったうえで**で、保険が狙うのは履歴の辻褄合わせではなく「**取り返しのつかない状態にしない**」ところまで。WKWebView ではネイティブ undo が勝ったまま、アプリの履歴と DOM はずれる([issues.md](../../issues.md) #89 に残した) | ①横取りだけ(WKWebView で uid を失った要素が残る) ②保険だけ(Chromium でも履歴とネイティブ undo が二重に効く) ③編集中は ⌘Z を捨てる(いちばんよく押されるキーを塞ぐ) | 反映済 |
| 92 | 2026-08-25 | **履歴の復元(Undo / Redo)が入れた markup は洗わない**。印は `useHistory` の `lastAt === 0` で立て、`withUndo` と同じくマイクロタスクで下ろす(#63 と同じ形) | 貼り付けを見張る MutationObserver は**復元の書き戻しも通る**。洗うと uid が総剥がしになり、さらに次の Redo が「uid だけ違う」余計なコマンドを積んで **redo を潰す**(実測: ⌘Z のたびに箱の中の uid が総入れ替えになり ⌘⇧Z が効かない / [issues.md](../../issues.md) #80)。復元される markup は**このアプリが以前に書き出したもの**なので、そもそも洗う理由が無い。`lastAt` は #77 が既に引いている線なので、2 つ目の状態を持たずに済む | ①復元の直前後で観測を止める(止めている間に届いた本物の貼り付けを取り逃す) ②`setTimeout` で印を下ろす(#63 の却下案と同じ — タスク境界まで開きっぱなしになる) ③洗浄側で「アプリが書いた markup か」を印で見分ける(書き出し HTML を汚すので [ADR-0001](../../adr/0001-html-as-source-of-truth.md) と衝突) | 反映済 |
| 93 | 2026-08-25 | テキストセッションの `tryMerge` は、**innerHTML から拾った uid の集合が同じときだけ**畳む(文字列を走査し、パースはしない) | 打鍵も書式コマンドも同じ `SetInnerHtmlCommand` で記録するので、「同じ uid なら畳む」だけでは両者を区別できず、**U を押して 150ms 後に S を押すと undoStack が 1 しか増えなかった**(1 秒以上あければ 2 増える / [issues.md](../../issues.md) #81)。区別は**触った要素の集合**で付く — タイピングは要素を増やさずテキストノードだけを変えるが、書式コマンドは `<u>` や `<span>` を新しく生むので uid が増える。パースしないのは、比べる 2 つが同じ作り方(`reindex` 直後の `innerHTML`)で作られているため。`innerHTML` は属性値の `"` をエスケープするので、パターンが当たるのは本物の属性か、スライドに**文字として打たれた** `data-hse-uid="…"` だけ — 後者は最悪でも併合の判断を 1 回間違えるだけなので、安いほうを採った | ①打鍵ごとに DOM をもう 1 つ建てて比べる(打鍵のたびにフルパースが乗る) ②書式コマンド側に「畳まない」フラグを渡す(`withUndo` を通る経路すべてに渡し忘れの余地が生まれる) ③併合窓(800ms)を短くする(速く打つ人の打鍵が畳まれなくなり、undo が 1 文字ずつになる) | 反映済 |
| 94 | 2026-08-26 | 移動中の Shift を**軸固定**に振り替え、**吸着オフのキーは置き直さない**。軸は押した位置からの移動量が大きいほうで、ポインタが動くたび決め直す。固定中は**止まっているほうの軸を吸着の候補に出さない**(`snapToGuides` の `edges` に空配列) | Shift はリサイズ(縦横比固定)・回転(15 度刻み)では PowerPoint と同じ意味なのに、移動だけ「吸着オフ」という別の意味だった([issues](../../issues.md) #97)。押す側からは同じ 1 つのキーで、3 つのうち 1 つだけ違う理由を説明できない。逃がし先を作らなかったのは、ドラッグ中に覚えるキーを 3 つに増やすほどの用途ではないため —— 吸着を避けたい 1px 単位の詰めは矢印キーが担う。止まっている軸を候補から外すのは、外さないと吸着が Shift の押さえている軸から引き剥がすから | ①吸着オフを ⌘ / Ctrl ドラッグへ逃がす(Mac 版 PowerPoint と同じ割り当てだが、キーが 3 つになる) ②Alt を吸着オフにして複製を ⌘ / Ctrl へ(Windows 版 PowerPoint の割り当て。Mac の Option ドラッグ複製の習慣と、既存の設計書・テストを全部書き換えることになる) ③軸固定中も 6 本すべて差し出す(固定が吸着に負ける) | 反映済 |
| 95 | 2026-08-26 | **リストを持てない編集ホスト(`<ul>` / `<ol>` / `<span>` / `<p>`)は、コマンドの前にホスト要素そのものを建て替える**。uid を引き継ぎ、タグが変わって失われた文字の宣言だけを computed 差分から書き戻す | `execCommand` はホストの**内側**にしかリストを建てられず・外せない。デッキが元から持つ箇条書きは遡上が `<li>` / `<ul>` を透過してスライドルートの手前で止まるので `<ul>` 自身がボックスになり、箇条書きも番号も空振りしていた([issues](../../issues.md) #98)。`<span>` は display に関わらず Blink が拒否し(`false` を返す)、`<p>` は `<p><ul>` の入れ子が再パースで分解される(同 #103) | ①編集ホストをスライドルートへ一段上げて `execCommand` に外側から見せる(実測: 解除が空の `<ul>` と 26px の span を残し改行が消える。キャレット 1 点でリストが 3 つに割れ、**1 つのボックスが 3 つに増える**) ②ホストが `<ul>` のときボタンを disabled にする(押せない理由は伝わるが、元からあるリストを外す手段が無いまま) | 反映済 |
| 96 | 2026-08-26 | **建て替えをまたぐキャレットは `Range` ではなく、境界のテキストノードと文字位置で持ち越す**。テキストノード以外を指していた選択は、建て替え後のホスト全体に倒す | `Range` はライブで、子ノードを別の親へ移した時点で境界が親へ繰り上がる(DOM 標準の removing steps)。`cloneRange()` でも同じで、要素を外すとさらに上へ逃げる —— 復元すると選択がスライドルートまで広がる。テキストノード自体は移動しても生き残り、文字位置は触っていないテキストの中の位置なので意味を保つ | ①`Range` を clone して持つ(実測で境界が箱の外へ出る) ②建て替え後は必ずホスト全体を選択する(2 回目の押下が、ユーザーが選んでいない範囲に効く) | 反映済 |
| 97 | 2026-08-26 | **テキストセッション中の選択枠は、線だけを要素の外へ逃がす**(`outline` + `outline-offset` で 3px 外。`border` は幅を保ったまま透明にして残す) | 枠は要素の境界に置かれ、`box-sizing: border-box` なので線は境界の**内側**に落ちる —— そこはキャレットが立つ場所でもある。空の箱ではキャレットもエディタの青(`placeholder.ts`)なので、同じ位置に同じ色が重なって塗り潰されていた。実測(Chromium・scale 0.72)では変更前は青が design px の 120〜122.78 を塗ってキャレットの x=120 を 2px 飲み込み、変更後は 113.5〜116 に下がって画面上 2.88px の隙間が空く | 枠を細く / 破線 / 半透明にする(キャレットと同じ位置にあることは変えられない)・`border` の幅を 0 にする(`background-clip` の既定が `border-box` なので塗りが 2px 縮む) | 反映済 |
| 98 | 2026-08-26 | **ユーザーが空にした箱は印(`data-hse-blank`)を残し、「エディタが text だと知っている箱」として扱う**。判定は `isKnownTextBox`(印の有無)で、編集ステージ限定の CSS で 24x16px の床を敷く | 印を外すとデッキ自身の空 `<div>` と区別が付かず、入口が 3 つとも閉じ・クリックが背後のパネルに登り・幅 0 に潰れて掴めなくなる。実測(ユーザー指定の実デッキ・262 のテキスト要素)で **262 件すべてが再編集不能、122 件が幅 0**。「今テキストを持っているか」を広げる案は #52・#75 で 2 度却下しているので、印なら**ユーザーが空にした箱にだけ**答えを変えられる | `hasOwnText` の条件を緩める(デッキの空 div がすべて編集可能になる)・空なら箱ごと削除する(デッキ由来の要素だとレイアウトが動き、戻す手段が Undo だけになる)・markup に印を書く(不変条件 2 に触れる) | 反映済 |
| 99 | 2026-08-26 | 操作の対象を**「写真枠」ではなく「選んだ箱」**に置く(「枠を見分けて中身を入れる」を「**選んだ箱の中身を画像に差し替える**」へ一般化)。可否を答える `isFillable` は `!isReplaced && !isCroppable && !isTextEditable` の**読み合わせにとどめ、3 つ目の試験にしない** | **枠だと見分ける印は持てない。** 印を付けるなら `data-hse-*` になるが、シリアライズが `data-hse-` で始まる属性を**全部落とす**(`stage/bridge.ts` の `cleanElement`)ので、**最初の保存までしか保たない** —— 開き直した瞬間に写真枠でなくなる機能は、無いよりたちが悪い。往復を越えて残るのは**箱そのもの**なので、それを対象にする。写真枠は「一番それを欲しがっている箱」でしかない([issues](../../issues.md) #100)。読み合わせにとどめるのは、**新しい「画像とは何か」を宣言しない**ため —— 宣言すれば `ElementKind`(`text` / `image` / `shape`)に 4 つ目が要り、遡上・トリミング・インスペクタがそれぞれ答えを持ち直す。3 つとも既に別の場所が答えている問いなので、そちらへ委ねる(パネル側の帰結は [inspector/design.md](../inspector/design.md) が持つ) | ①取り込み時に `data-hse-*` で印を付ける(**保存で剥がれる**ので、次に開いたときには枠でなくなる) ②構造(破線 + `inset:0` + キャプション)で見分ける(`materializeImageSlots` が**今日たまたま書いている** markup にこのモジュールを縛る。デッキの作りが変われば黙って当たらなくなる) ③`isFillable` を種別として立てる(既存の 3 試験と食い違ったときに、どちらが正か決める場所が無い) | 反映済 |
| 100 | 2026-08-26 | ダブルクリックの分岐で、**画像を入れる判定を `textual` の後ろに置く**。2 つの試験を 1 つの条件に混ぜず、**順番で決める** | 挿入直後の空テキストボックスは文字も画像も持たないので **`isFillable` を満たしてしまう**。だがそこは**打ち込むために作られた箱**で、ダブルクリックは「ここに打つ」でなければならない(`isUntouchedTextBox` / #83)。`\|\|` で 1 つの条件にすると、どちらが勝つかが式の形に埋もれる。順番なら「文字が先、画像はその次」がそのまま読める | ①`isFillable` の側に「挿入直後の箱を除く」を足す(#75 / #83 の却下案と同じ形 —— 「エディタがテキストだと知っている箱」の定義を触る場所が 2 つに増える) ②ダブルクリックを画像専用にし、テキスト編集は Enter / F2 と右クリックに任せる(#59 で確かめた入口を 1 つ潰す) | **差し替え(#109)** |
| 101 | 2026-08-26 | ドロップの「ここに落ちる」表示に**ホバー枠を流用する**(専用のオーバーレイを足さない) | ドラッグ中は **CSS が効かない** —— OS がポインタを握るので `:hover` すら来ず、要素にクラスを当てて光らせる手も無い([ADR-0011](../../adr/0011-native-file-drop.md))ので、**ホスト側で描くしかない**。枠は元から「ポインタが指しているもの」の意味で、**OS がポインタを握っているあいだ本物のホバーは起きない**ので、意味も描画も衝突しない。1 本で足りるものを 2 本描くと、色・太さ・ズーム追従を 2 箇所で合わせ続けることになる | ①ドロップ専用のオーバーレイを足す(見た目の規則が 2 箇所に分かれ、両方をズームに追従させることになる) ②スライド側の要素にクラスを当てて CSS で光らせる(足したクラスはシリアライズの対象になり、不変条件 2 に触れる) ③何も出さない(受け入れられない場所に落ちたときは黙って無視する仕様なので、**落ちる先が見えないと何が起きたか読めない**) | 反映済 |
| 102 | 2026-08-26 | **移動は「見える範囲」(スライド ∩ 祖先の `overflow`)に要素の 24px を残す位置で止める**。判定は吸着のあと、現在位置から測る | 拾い直す手段をいくら足しても、**ドラッグのたびに見失う**ことは変わらない。祖先の `overflow:hidden` は描画だけでなく**ヒット判定からも**要素を外すので、カードの外へ出た瞬間「どこにも無い」ものになる([issues](../../issues.md) #102。実測: 見出しは (706,162)-(1214,258) にあり、ポインタは中心にあるのに `elementsFromPoint` の 6 要素に居ない)。スライドルート自身も取り込みが `overflow:hidden` を敷く(`deckStage.ts`)ので、**同じ規則がスライドの外へのドラッグも塞ぐ**。現在位置から測る副産物として、**すでに外に出ている要素は最初の 1px で戻る**(実測: 重なり -139px → 24px) | ①**祖先の `overflow` を上書きして見えるようにする** —— ユーザーが選んでいない要素への編集になり、角の丸めや他の子のはみ出し方まで変わる(不変条件 2)。書き出しにも載るので、編集画面と書き出しを一致させるには載せるしかなく、**ユーザーがしていない編集がデッキに残る** ②**DOM ごとスライド直下へ移す** —— 元の位置のレイアウト枠が消えてカードが詰まり、デッキの CSS の当たり方も変わる(不変条件 2 ③) ③**何もせず拾い直す手段だけ置く** —— 今回の報告そのもの | 反映済 |
| 103 | 2026-08-26 | **「同じ点にあるもの」の列は 2 段で作る** —— ヒット判定が届くものを**描画順**で先に、届かないもの(切り取り・`pointer-events:none`)を**矩形で拾って文書順の逆**で後ろに(`bridge.elementsAt`) | `elementsFromPoint` だけでは切り取られた要素に届かない。かといって全部を矩形で測ると `z-index` を無視することになる —— **前半では本物の描画順がすでに答えている**ので、そこは壊さずに後ろへ足すだけにした。デッキが `visibility:hidden` で隠しているものは列に入れない(ユーザーが動かしたものとは別物) | ①全部を矩形で測る(重なりの順序が `z-index` を無視する) ②`elementsFromPoint` だけ(報告された症状に届かない) ③`display:none` も拾う(矩形を持たないので、そもそも「その位置にある」と言えない) | 反映済 |
| 104 | 2026-08-26 | **ジェスチャは 1 歩ずつ、メニューは一覧。** Alt+クリックは列を 1 歩進め(潜るかどうかは**押した瞬間ではなく離した瞬間**に決める)、右クリックは「この位置にあるもの」として列をそのまま並べる | 離した瞬間に決めるので、Alt ドラッグ(複製)は 1 行も変えずに済む —— デッドゾーンを越えていれば複製、越えていなければ潜る。メニューを 1 歩にしなかったのは、**開いた時点で選択肢が確定するものだから** —— 実測すると 1 歩の行き先はクリック位置で変わる(見出しの文字の上なら `h3`、外なら見出しの箱)ので、名前を見ずに選ばせると当たりも外れも説明できない。繰り返せるジェスチャは外れても次の 1 歩で直る | ①Alt+クリックを押下時に判定(Alt ドラッグの複製が、見えているものではなく奥のものから始まる) ②メニューも「奥の要素を選ぶ」の 1 項目(**普通の場所では「親へ」と同じ動きに化ける** —— 重なりの無い点では列は祖先の連なりそのもの) ③同じ場所の連打で潜る(ダブルクリック = テキスト編集と取り合いになる) | 反映済 |
| 105 | 2026-08-26 | **選択枠の四辺に見えない 7px の掴みしろを敷き、押すと選択中の要素の移動を始める**(`MoveGrips`)。**辺だけで中は塗らない**。ハンドルより先に描く | 枠もリサイズハンドルもホスト側に描いてある(不変条件 15)ので、下の要素が切り取られていようが覆われていようが届く —— **動かせないのにリサイズはできる**という半端さのほうがおかしかった。中まで掴みしろにすると、**選択中のパネルの上に載っている小さな要素が二度とクリックできなくなる**(選択が自分の領域のクリックを全部食う)。ハンドルより先に描くのは、角と辺の中点で両方が重なるため(後に描いたほうが勝つ) | ①**選択の矩形の中ならどこでも移動を始める**(上に載っている要素が選べなくなる) ②**ヒット判定で選択に届かないときだけ内側も掴みしろにする**(「覆われている」と「選択の上に別の要素がある」を区別できず、①と同じ結果になる) ③矢印キーとインスペクタの座標欄だけで動かす(**戻す作業にならない**、というのが今回の差し戻し) | 反映済 |
| 106 | 2026-08-26 | 要素のクリップボードを復活させ、**貼り付け先を「選択した要素の次の兄弟」**にする。選択が無いときだけスライドルート直下 | 取り下げの理由はキーではなく貼り付け先だった([roadmap](../../roadmap.md))。次の兄弟なら**親が変わらない**ので、grid のセルや `.container .card` で塗られていた箱がそのままの見た目で増える —— 実ブラウザで測ると、同じ親・同じ塗り・同じ余白・同じ幅で 3 列目に収まり、スライド直下へ落とした同じカードは塗りが `rgba(0,0,0,0)` に落ちる。**どちらの経路になるかを押した時点の選択で決める**ので、規則を増やさずにユーザーが選べる | ①**常にスライドルート直下**(旧実装そのもの。取り下げの原因) ②**コピー元の親へ戻す**を間に挟む 3 段(uid はステージが発行するので**別スライドでは結局 3 段目に落ちる**。分岐だけ増えて答えは変わらない) | 反映済 |
| 107 | 2026-08-26 | **ずらすのは「自分で位置を持つ要素」が「まだ何か居る場所」に貼られたときだけ。** 位置の決まり方(computed の `position`)は**コピーした時点で測って覚える** | 条件を「元がまだ居るか」だけにしていたとき、実ブラウザで 3 列 grid の 3 列目に入ったカードが**行から 16px 下がった**(y=84 と y=68)。セルには収まっているのに隣と揃わない —— 「元をよける」つもりのずらしが「自分のセルから出る」になっていた。親が置き場所を決める要素はコピーが自分のセルをもらうので、そもそも元の上に重ならない。貼る時点で残っているのは文字列だけなので、計測はコピー時にしかできない | ①貼った直後に矩形を測って重なりを見る(jsdom は矩形が全部 0 なので**ずらしの検証が unit から落ちる**) ②ずらしを一切やめる(自分で位置を持つ要素が元の真下に隠れ、貼れたことが分からない) ③貼り付け先が同じ親のときだけずらす(絶対配置なら別の親へ貼っても元の上に来るので外れる) | 反映済 |
| 108 | 2026-08-26 | **OS のクリップボードは経由しない。** モジュール変数 1 本で持つ | 運ぶのは要素の markup で、他アプリが受け取っても意味を持たない。OS 経由にすると**外から入ってきた HTML を洗う**責任が増え、それはテキスト編集中の貼り付け(`paste.ts`)がすでに別の規則で持っている。書式のコピー(`format.ts`)も同じ形で、揃えたほうが読み手の予想が当たる | ①`navigator.clipboard` に `text/html` として書く(別ウィンドウ・別アプリと行き来できるが、洗浄の責任と非同期の失敗経路が増える) | 反映済 |
| 109 | 2026-08-26 | **画像の挿入をダブルクリックから右クリックメニューへ移す**(#100 の差し替え)。ダブルクリックはテキスト編集とトリミングだけになり、「画像を入れる」は行に名前のあるメニュー項目になる | ダブルクリックは**箱の中身によって意味が変わる**ジェスチャだった —— 文字ならテキスト編集・画像ならトリミング・どちらでもなければファイルダイアログ。しかも「どちらでもない箱」は**見ただけでは挿入直後の空テキストボックスと区別が付かない**。#100 が `textual` を先に見る**順番**で凌いだこと自体が、1 つのジェスチャに 2 つの意味が乗っている証拠だった。メニューなら行に名前があるので読み違えようがなく、**順番の縛りも消える** —— 空テキストボックスでは「テキストを編集」と「画像を入れる」が両方出て、どちらのつもりだったかはユーザーが選ぶ。#104 で「ジェスチャは 1 歩ずつ、メニューは一覧」と決めたのと同じ線引き | ①ダブルクリックを残したままメニューにも足す(意味が中身で変わる問題はそのまま。入口が増えるだけ) ②ダブルクリックを画像専用にする(#100 の却下案 ②と同じ —— テキスト編集の入口が 1 つ減る) ③長押しやクリック 1 回にする(移動・選択のジェスチャと衝突する) | 反映済 |
