---
feature: i18n
status: active
updated: 2026-08-25
---

# i18n — 判断ログ

蒸し返し防止用。ここは「まだ仕様に昇格していない決定」と「経緯」の置き場。仕様の正は design.md。
全体に影響する判断はここではなく [../../adr/](../../adr/) へ。

## 記録基準

- **書く**: 議論して決めたこと / 代替案を退けたこと / 後で「なぜこうした？」になりそうなこと
- **書かない**: 自明な判断、コードを見れば分かること

## 運用

- 判断が覆ったら行は消さず、状態を `上書き → #n` にする（覆った経緯こそ蒸し返し防止に効く）
- 判断が仕様として安定したら **design.md 本文に反映**し、状態を `反映済` にする

## ログ

| # | 日付 | 判断 | 理由 | 見送った代替案 | 状態 |
|---|---|---|---|---|---|
| 1 | 2026-08-23 | `i18next` を入れず自作する | 要るのは**キーを引くことと `{name}` の補間だけ**。複数形・文脈・名前空間・遅延読み込みは現状どこにも要らない（[rules/development](../../rules/development.md) §6「数十行で自作できるなら自作する」）。仕組みそのものは `index.ts` と `locale.ts` の 110 行で、残りはカタログ（`ja.ts`） | `i18next` / `react-intl` を追加する | 反映済 |
| 2 | 2026-08-23 | キーは**ドット区切りのフラット**（`'toolbar.import'`） | grep で追える。`t('toolbar.import')` を 1 回検索すれば呼び出し側とカタログの両方に当たる | ネストしたオブジェクト（呼び出し側の文字列が `ja.ts` のどこにも現れなくなる） | 反映済 |
| 3 | 2026-08-23 | `t()` はフックにせず、素の関数にする | Command のラベルは「元に戻す」ボタンのツールチップに出る（`src/features/Toolbar.tsx:111`）**UI 文言でありながら `core/` にある**。`core` は React に依存できない（[05-directory](../../basic-design/05-directory.md) の依存方向）ので、フックでは引けない | `useT()` フック / Context 経由で配る | 反映済 |
| 4 | 2026-08-23 | `t()` は**呼ぶたびカタログを引き直す**（モジュール読み込み時に値を固定しない） | 言語切替を「再描画」だけで済ませられる形にしておく。ただし `readonly label = t('command.editText')` は**インスタンス構築時**に確定するので、`initLocale()` は render より前に置く（`src/main.tsx:9-11`） | 起動時に全文面を定数へ焼き付ける | 反映済 |
| 5 | 2026-08-23 | 他言語カタログを**日本語カタログの型に対して**型付ける（`Record<Locale, Record<MessageKey, string>>`） | 後から足す言語がキーを 1 つ落としたら**型エラー**になる。空ラベルをユーザーが見つける形にしない | 実行時にキーの差分を検査する | 反映済 |
| 6 | 2026-08-23 | 値が渡されなかった `{name}` は**そのまま残す**。空にしない | 見えている `{label}` は**どの呼び出しが壊れているか**を名指しする。空文字は何も言わず、単なる描画バグに見える | 空文字に落とす / 例外を投げる | 反映済 |
| 7 | 2026-08-23 | 表示言語は `localStorage` の `hse.locale` に置く。プロジェクトにも書き出す HTML にも入れない | ペイン幅と同じ**ワークスペースの好み**であって、ドキュメントの内容ではない（[07-ui-system](../../basic-design/07-ui-system.md)）。同じ機械で開けば同じ言語、渡した HTML には何も付かない | プロジェクトファイルに持つ（渡した相手の言語まで固定してしまう） | 反映済 |
| 8 | 2026-08-23 | 保存値 → `navigator.language` → 既定、の順で決める。**カタログのある言語だけ**を候補にする | 明示した選択が機械の設定に勝つ。カタログの無い言語（フランス語など）に一致させると**キーがそのまま画面に出る**ので、日本語へ落ちるほうがまし | `navigator.language` を常に優先する | 反映済 |
| 9 | 2026-08-23 | `localStorage` の読み書きが失敗したら**握りつぶす** | プライベートモードのストレージ失敗で**起動できない**ほうが困る。落ちるのは「前回の選択を覚えていない」ことだけ | 例外を投げる / メモリ上のフォールバックを持つ | 反映済 |
| 10 | 2026-08-23 | 今回入れるのは**仕組みと日本語カタログまで**。英語は入れない | 翻訳と抽出は別の作業で、混ぜると「文面がおかしいのか、参照が壊れているのか」を切り分けられない。加えて当時は **smoke が 169 箇所で日本語の UI 文言に照合**していたので、既定を日本語に保てば**抽出が正しいことを既存のテストがそのまま証明**した | 同じ変更で英語カタログも入れる | 反映済（英語は [issues](../../issues.md) #15 で追う） |
| 11 | 2026-08-23 | 書体名・`PROBE_TEXT`・`TYPING_HEADROOM` はカタログに入れない | 書体名は**固有名詞**で、訳すとどの書体か分からなくなる。あとの 2 つは UI ではなく**測定用の文字列**なので、訳したら機能が壊れる（[fonts](../fonts/design.md) / [import-pipeline](../import-pipeline/design.md)） | `src/` の日本語リテラルを 1 件残らずカタログへ寄せる | 反映済 |
| 12 | 2026-08-23 | 区切り記号（`、`）と語尾（`{items}しました`）も**カタログの項目にする** | 文の組み立ては装飾ではなく**文法**。呼び出し側に `join('、')` と `+ 'しました'` を書くと、そう文を作らない言語で直す場所が無くなる（`src/app/actions.ts:64`） | 呼び出し側で連結する | 反映済 |
| 13 | 2026-08-23 | 「画面にキーがそのまま出ていない」の**自動検査は入れない** | 当時 smoke に足そうとしたが、**わざとキーを直書きしても赤くならなかった** —— 検査した時点でその文言が画面に出ていないだけで通ってしまう。守れていない検査は入れない（[rules/development](../../rules/development.md) §2）。代わりに `t()` の引数型（#5）で綴りを保証する | smoke に「キーが出ていない」検査を足す | 反映済（手で見る側は [test.md](test.md) #7・#8） |
| 14 | 2026-08-24 | 空のテキストボックスの「テキストを入力」は**カタログに入れる**（`stage.textBoxPlaceholder`） | かつては挿入する markup の中身そのもの（`content.newTextBox`）で、「どうせ書き換えられる」前提だった。実際には書き換えられず**エディタが選んだ言葉がユーザーの書き出した HTML に残った**。ステージが `::before` で描く chrome に変えたので、今は保存する markup に一切現れない（[editing-engine](../editing-engine/design.md)、[issues](../../issues.md) #34） | markup の初期値として持ち続ける（上書き前提） | 反映済 |
| 15 | 2026-08-25 | 言語切替 UI は入れない。`setLocale()` は口だけ用意し、呼び出し元を持たない | カタログが 1 つしか無い状態で切替 UI を出しても、選べるものが 1 つしか出ない。2 つ目のカタログが入った時点で作る | 先に設定画面を作っておく | 未着手（[design.md](design.md) 実装タスク） |

**#10 の後ろ盾は、smoke を廃止した時点で無くなった**（[ADR-0009](../../adr/0009-drop-smoke-e2e.md)）。
英語カタログを足すときに「文言が正しく差し替わったこと」を自動で示すものはもう無いので、
**画面の文言は手で確かめる**（[test.md](test.md) の `手動` の行）。
