---
feature: image-crop
status: active
updated: 2026-08-24
---

# image-crop — 判断ログ

蒸し返し防止用。仕様の正は [design.md](design.md)。

## ログ

| # | 日付 | 判断 | 理由 | 見送った代替案 | 状態 |
|---|---|---|---|---|---|
| 1 | 2026-08-21 | **`clip-path: inset()` を採らない**。枠 + 画像の 2 矩形で書く | `clip-path` は描画を切るがレイアウトボックスは元のままで、`getBoundingClientRect` が切る前の矩形を返し続ける。選択枠・スナップ対象・整列コマンドがすべて見た目とズレる。**辺を掴めないトリミングはトリミングではない** | `clip-path: inset()`／canvas で焼き込む(はみ出しを捨ててしまう) | 反映済 |
| 2 | 2026-08-21 | 枠かどうかは**エディタ固有のマークではなく形で判定**する(クリッピングする箱 + 唯一の子が絶対配置の `<img>`) | `data-hse-*` の目印はシリアライズで消えるので、書き出して読み直したファイルでトリミング状態が失われる。形で見れば手書きのデッキもトリミングできる | `data-hse-crop` のような印を付ける | 反映済 |
| 3 | 2026-08-21 | `<img>` を `<div>` で包むのは**トリミングを開始した瞬間だけ**で、独立した undo ステップにする | 頼まれてもいない構造変更なので、ユーザーが 1 回の Undo で元の `<img>` に戻せる必要がある | 常に包んでおく／包み込みをトリミング操作と同じステップにまとめる | 反映済 |
| 4 | 2026-08-21 | 開始時に `object-fit` の描画矩形を明示的な `left/top/width/height` に**書き直し、`fill` に固定**する | 見た目を 1px も変えずに 2 矩形モデルへ移行するため。書き直したあとは `object-fit` に仕事が無い(同じ比率では同じ描画) | `object-fit` を保ったまま扱う(ジェスチャの計算が全ケース分岐する) | 反映済 |
| 5 | 2026-08-21 | `contain` で余白がある場合は**枠を実画像まで詰める**(背景・枠線・影があるときは詰めない) | 詰めないと「画像より枠のほうが大きい」状態から始まり、不具合に見える。エディタの「画像を挿入」がこのケース。ただし背景・枠線・影を持つ画像は余白にも描いているので詰めてはいけない | 常に詰める(装飾を切る)／決して詰めない(不具合に見える) | 反映済 |
| 6 | 2026-08-21 | 枠のリサイズは**画像も一緒に拡大縮小する** | 枠だけ変えると、リサイズが黙って再トリミングになってしまう。PowerPoint も同じ挙動 | 枠だけ変える | 反映済 |
| 7 | 2026-08-21 | **確定だけがあり、取り消しは無い。** Enter も Esc も「終了」 | やり直しは Undo で足りる。PowerPoint と同じ割り切り。トリミング専用の取り消しを持つと、Undo との関係が二重になる | Esc をトリミング前に戻す取り消しにする | 反映済 |
| 8 | 2026-08-21 | トリミング中のクリッピング解除は**ステージのスタイルシート**(`[data-hse-cropping]`)で行い、インラインスタイルに書かない | 保存されるのは要素の `style` 属性のほう。インラインに書くとセッションの状態が書き出しに漏れる | インラインで `overflow: visible` を書く | 反映済 |
| 10 | 2026-08-21 | 解除は**開始前の `style` を復元する**。エディタが作った枠はそのとき取り除き、借りた枠・デッキ製の枠は外さない | 包み込みは `width:100%` などの相対サイズを px に焼き付けるので、枠を残したままでは画像が親の箱に追従しない状態が続く(取り込んだデッキの画像がこの形)。一方デッキ製の枠は著者の要素で、外すと著者の styling ごと消える | 常に広げるだけ(追従が戻らない)／常に外す(デッキの markup を壊す) | 反映済 |
| 11 | 2026-08-21 | 戻し先は **`data-hse-crop-origin` を書き換える要素それぞれに退避**し、エディタが作った枠には **`data-hse-crop-owned`** を付ける。**どちらも枠かどうかの判定には使わない** | `serializeSlide` は `data-hse-*` を落とすので書き出しに漏れず、`slideMarkup()` は残すので undo / redo をまたいでも失われない。判定に使わないので #2「形で判定」は維持される。属性を 2 つに分けるのは「戻し先がある」と「外してよい」が別の事実だから(借りた枠は前者だけを満たす) | セッション state に持つ(セッションを閉じると失われる)／1 つの属性で兼ねる(借りた枠を外してしまう) | 反映済 |
| 12 | 2026-08-21 | 解除では**サイズは戻し、スライド上の位置は残す** | 枠にしてから動かした分はトリミングではない。「元の画像に戻す」はサイズと絵柄の話で、置いた場所まで巻き戻すのは別操作 | 位置も含めて全部戻す/位置もサイズも残す(何も戻らない) | 反映済 |
| 13 | 2026-08-21 | `pictureOf()` は **`data-hse-cropping` を持つ要素をクリッピング済みとして扱う** | セッション中の `overflow: visible !important` が author-important としてインライン宣言に勝ち、computed style だけでは自分が作った枠を見失う。1 回ドラッグして commit した瞬間にインスペクタの「画像」パネルが消えていた。外したのはステージ自身なので、その分だけ補う | セッション中はインラインで `overflow` を書き換える(#8 に反し書き出しに漏れる)／パネル側で例外処理する(同じ穴が他の呼び出し元にも残る) | 反映済 |
| 14 | 2026-08-22 | 開始時、画像がすでにクリッピングする箱に**単独で**入っているなら、**その箱を枠として借りる**。新しい `<div>` を作らない | 取り込んだデッキの画像はこの形。中にもう 1 枚挟むとデッキ側の箱がトリミング前の大きさのまま残り、「トリミング」ではなく「画像が縮んだ」ように見える。しかも 2 回目のダブルクリックで 3 枚目が増える(`openCrop` が選択中の `<img>` しか見ていなかった) | 常に包む(入れ子になる)／`data-hse-*` の印で自分が作った枠だけ再利用する(デッキ製の枠には効かない) | 反映済 |
| 15 | 2026-08-22 | **借りた枠は詰めない**(#5 の例外) | 著者が選んだ大きさの箱で、レターボックスの帯にも自前の板を描いている。詰めると著者のレイアウトを書き換えてしまう | 借りた枠も詰める | 反映済 |
| 16 | 2026-08-22 | **トリミング済みの画像のクリックは枠を選ぶ**。判定は `chooseSelectionTarget`(editing-engine)側に置く | 画像は枠より意図的に大きいので、画像を選ぶと**トリミング前の矩形**に選択枠とハンドルが出て、ドラッグが枠内スライドになる。design.md の「枠がスライド上のオブジェクト」と食い違っていた。全面写真が背景扱いで選べなくならないよう、枠も「中身を持つもの」に数える | 選択後にインスペクタ側で読み替える(ハンドルとドラッグは直らない) | 反映済 |
| 17 | 2026-08-22 | **セッションの印(`data-hse-cropping`)を markup スナップショットに入れない** — 解除はセッションを閉じてから記録する | 印を含んだ markup を undo で書き戻すと、セッションが無いのに印だけ残った枠ができ、`overflow: visible !important` が効いたまま**二度とクリッピングしない**。トリミング前の画像がはみ出したまま残る | スナップショットを取り込むときに印を落とす(`slideMarkup` は「何も掃除しない」契約) | 反映済 |
| 18 | 2026-08-23 | `pictureOf()` は**スライドルートを枠として認識しない** | 生成デッキはスライドを `overflow:hidden` でクリップし、表紙は全面写真 1 枚 — 枠の構造条件をそのまま満たす。すると `frameOf()` が画像のクリックをスライドルートへ昇格させ、「スライドは選択しない」規則に当たって `null` に化け、**画像が選択もトリミングもできない完全な無反応**になっていた。同じ規則は `borrowableFrame()` には入っていて、認識側にだけ無かった | 遡上側(`frameOf`)だけで弾く(`isCroppable` は誤判定のまま残る) | 反映済 |
| 9 | 2026-08-21 | 減光の帯は**ホスト側のオーバーレイ**に描く | ステージの中に描くと書き出し HTML に混ざる(選択枠・ハンドルと同じ理由) | ステージ内に要素を足す | 反映済 |
| 19 | 2026-08-24 | **グリップのデッドゾーンは `GestureController` と同じ数字を共有する** — 換算ごと `deadZoneFor(scale)` に出し、`CropController` はそれを呼ぶ | 掴んで離すだけで履歴に「トリミング」が積まれ、指の震え分だけ実際に切れていた([issues](../../issues.md) #30)。移動・リサイズ・回転で直したもの(#18 / #29)と同型で、`CropController` だけが外にいた。しきい値を 2 箇所に持つと片方だけ調整されてズレるので、値ではなく**換算関数**を共有する(ズーム換算の `Math.max(scale, 0.01)` も 1 箇所で済む) | `cropGesture.ts` に 4px を書き直す(同じ数字が 2 箇所)／`shared/` に定数だけ切り出す(換算式は結局重複する)／トリミングにはデッドゾーンを入れない(症状が残る) | 反映済 |
