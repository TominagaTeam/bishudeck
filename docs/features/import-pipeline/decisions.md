---
feature: import-pipeline
status: active
updated: 2026-08-26
---

# import-pipeline — 判断ログ

蒸し返し防止用。仕様の正は [design.md](design.md)。
全体に影響する判断は [../../adr/](../../adr/) へ。

## ログ

| # | 日付 | 判断 | 理由 | 見送った代替案 | 状態 |
|---|---|---|---|---|---|
| 1 | 2026-08-18 | `<script>` は**除去せず保持し、編集モードでだけ無効化する** | 判定不能なスクリプトを消すと再現性が壊れる。安全側 = 保持 | 未知のスクリプトを除去する | 反映済(2026-08-25 追記: **判断は生きているが、実装の場所が取り込み側ではなく合成側だった。** `compose.ts` の `neutralizeScripts` が edit モードで全 `<script>` を一律に無効化する。当初この行が書いていた「取り込み時のタグ付け」と framework / content の分類は実装されておらず、[issues #50](../../issues.md#i50) の裁定で design.md から落とした) |
| 2 | 2026-08-18 | Artifacts のローダー展開は**文字列処理として先に**行う | 編集モードで JS を実行しない(ADR-0002)以上、ローダーを走らせてスライドを得ることはできない | 取り込み時だけ一時的に JS を実行する | 反映済 |
| 3 | 2026-08-18 | テンプレートランタイム(x-dc / React)は**捨てる**が、プレゼンランタイム(`<deck-stage>`)は**保持する** | 前者はエディタが引き取る DOM を組み立てるもので、残すとエディタの下でデッキが自分を再描画する。後者は与えられた子要素を見せるだけで、編集結果を作り直さない | 両方捨てる(書き出しが全スライドを縦に並べた別物になる)／両方残す(編集中に再描画される) | 反映済 |
| 4 | 2026-08-18 | `{{ 変数 }}` は宣言された **`default` に置換**する。解決できないものは `{{ }}` のまま残す | `renderVals()` を実行するのは ADR-0002 が禁じている。誰も値を渡していない standalone では既定値がランタイムの描く値と一致する。空にすると**何が入るはずだったか読めなくなる** | 空文字に置換する／`{{ }}` を全部残す | 反映済 |
| 5 | 2026-08-18 | `<image-slot>` は**同じ見た目のプレースホルダに置き換える**(残さない) | shadow DOM を持つカスタム要素なので、ランタイムが無いと**ボックスを持たない未知のインライン要素**になる。空要素として残るのではなく画面から消え、選択すらできない | そのまま残す(不可視になる)／削除する(何があったか分からなくなる) | 反映済 |
| 6 | 2026-08-18 | `deckRuntime` は**モードで出し分けるのではなく、編集用モデルから外して持つ** | シェルに置いたままモードで出し分けると、置き場所が 2 つの意味を兼ねる。外して持てば「書き出しにだけ戻す」が 1 箇所で表現できる | シェルに残してモード別に無効化する | 反映済 |
| 7 | 2026-08-18 | `deckRuntime` の発見は自前マーカーではなく `customElements.define('deck-stage', …)` の**実体**で行う | 書き出した HTML は「インライン script を持つ普通の deck-stage デッキ」で、手書きのものと区別が付かない。それを読み直しても同じプロジェクトになる必要がある | 書き出し時に自前の印を付けて次回それを探す(手書きデッキを取りこぼす) | 反映済 |
| 8 | 2026-08-18 | Artifacts 展開は `isArtifactHtml()` を通ったファイル**だけ**に限る | 普通のデッキを parse → 再シリアライズの往復に通さないため。触る必要のない markup を書き換えるのは ADR-0001 が禁じている壊し方そのもの | 全ファイルを一律で正規化する | 反映済 |
| 9 | 2026-08-21 | `isArtifactHtml()` は**文字列ではなく要素**で判定する(部分文字列は前段の足切りとして残す) | エディタが書き出した deck-stage デッキは、足場 CSS のセレクタとしても usage コメントとしても `component-from-global-scope="deck-stage"` を持つ。文字列一致だと自分の書き出しを Artifacts と誤認して再展開し、**サブセット済みフォントをもう一度削る**(消えた文字のフェイスは戻らない) | 文字列一致のみ(誤認する)／常に要素判定(数 MB を毎回 parse する) | 反映済 |
| 10 | 2026-08-18 | 書き出しに残すかどうかは「エディタ由来か」ではなく**3 条件**(見た目不変・往復固定点・セレクタの当たり方不変)で判定する | 「エディタ由来のものは全部消す」だと、ランタイムを持たない deck-stage デッキで**スライドが潰れる**足場 CSS まで消えてしまう | エディタ由来を一律除去する | 反映済 |
| 11 | 2026-08-18 | 往復固定点のための印は `data-hse-injected` の**1 つだけ**にする | 印が増えるほど「取り込み時に外すもの」の管理が増える。生成が決定的なので、印の無い旧ファイルは出力との**完全一致**で照合すれば足りる | 要素ごとに別々の印を付ける | 反映済 |
| 12 | 2026-08-18 | スライドを抜くとき、**直前の空白テキストノードも一緒に落とす** | シェルが持つ空白と合成が挟む改行が二重になり、開いて保存するたびに 1 文字ずつ太る(固定点が崩れる) | 空白をそのまま残す | 反映済 |
| 13 | 2026-08-18 | フォントは**サブセットして**取り込む | 日本語ファミリは ~120 分割の 7MB で届き、共有 head はサムネイル 1 枚ごと・自動保存 1 回ごとに再シリアライズされる。実測 7.67MB → 1.12MB | 丸ごと取り込む(毎回 7MB を再シリアライズ)／フォントを捨てる(見た目が変わる) | 反映済 |
| 14 | 2026-08-18 | Detector の `runtimeCss` は共有 head の**先頭**に置く | デッキ自身の CSS が後に来て勝つので、**スライドの markup には一切触れずに**「ランタイムが無い状態」を埋められる | スライドの markup にクラスを足す(ADR-0001 違反) | 反映済 |
| 15 | 2026-08-25 | 分割は**確信度によらず必ず確認する**。閾値フラグ(`confident` / `CONFIDENCE_FLOOR`)は削除した | 失敗の重さが非対称。確認 1 枚は数秒だが、間違った分割は**手で戻すのが最も苦しい**取り込み失敗になる。閾値を持つと「高確信なら黙って通す」経路がいつか生えるが、それは払う価値のない賭け | 閾値を UI に繋いで高確信時は確認を飛ばす(失敗が高くつく側に倒れる)／フラグだけ残す(誰も読まないデッドコードが仕様に見える) | 反映済(フラグは実装当初から UI に繋がっておらず、[issues #50](../../issues.md#i50) の調査で発覚して削除) |
| 16 | 2026-08-26 | 写真枠のプレースホルダの**キャプションはクリックを通す**(`pointer-events:none`) | 枠全面を覆うキャプションが**すべてのクリックを受け止め**、そこで選択が止まって枠そのものに手が届かなかった([issues #100](../../issues.md#i100) の「操作が直感的でない」の正体)。**これは元の挙動に戻す変更**でもある —— `<image-slot>` はキャプションを shadow DOM に描いていて外から掴めないのが元の姿で、取り込みが素の `<div>` に開いたことで掴めるようになってしまっていた。破線のリングには最初から同じ宣言があるので、**揃えただけ**でもある。不変条件 2 は 3 条件とも満たすと判定(①見た目は変わらない ②宣言が 1 つ増えるだけで往復で増えない ③デッキの CSS / JS が知らない markup なので当たり方は変わらない。詳細は [design.md](design.md) の「写真枠のキャプションはクリックを通す」) | 掴めるままにして枠はパンくずからだけ選ばせる(#100 が残る)／キャプションを枠全面から縮める(プレースホルダの見た目がコンポーネントと変わる)／選択側でキャプションを特別扱いする(取り込みが作った箱の都合を選択ロジックに持ち込むことになる) | 反映済(**引き換えに、書き出した HTML ではそのキャプション文字を選択・コピーできなくなる**。掴めなかった元の姿に戻すほうを取る、とユーザーの裁定を得て受け入れた妥協) |
