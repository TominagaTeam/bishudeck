---
feature: persistence
status: active
updated: 2026-08-23
---

# persistence — 判断ログ

蒸し返し防止用。ここは「まだ仕様に昇格していない決定」と「経緯」の置き場。仕様の正は design.md。
全体に影響する判断はここではなく [../../adr/](../../adr/) へ。

## 記録基準

- **書く**: 議論して決めたこと / 代替案を退けたこと / 後で「なぜこうした？」になりそうなこと
- **書かない**: 自明な判断、コードを見れば分かること

## 運用

- 判断が覆ったら行は消さず、状態を `上書き → #n` にする（覆った経緯こそ蒸し返し防止に効く）
- 判断が仕様として安定したら **design.md 本文に反映**し、状態を `反映済` にする。AI が普段読むのは design.md 側で、ここは「なぜ？」と遡るときだけ読む

## ログ

| # | 日付 | 判断 | 理由 | 見送った代替案 | 状態 |
|---|---|---|---|---|---|
| 1 | 2026-08-23 | **一度も書き出していないデッキは自動保存しない** | 保存先を選ぶのはユーザーの判断で、勝手にどこかへ書くわけにいかない。代わりに閉じるときに 3 択で捕まえる | 既定の場所(書類フォルダ等)へ黙って書く(ユーザーが知らないファイルが増える) | 反映済 |
| 2 | 2026-08-23 | 終了確認は **2 択ではなく 3 択**(キャンセルを足す) | OS 標準の「保存 / 保存しない」だと、× の押し間違いに逃げ道が無い。セッションを捨てるか、選んでいない場所へ書くかのどちらかになる | OS の標準ダイアログをそのまま使う | 反映済 |
| 3 | 2026-08-23 | 「保存済み」と記録するのは**書いている間にドキュメントが動いていないとき**だけ | 動いていたのに `markSaved` すると、「書き出し済み」と出ているのにファイルには無い変更が生まれる。`project` の同一性で判定する | 書き込み成功をもって常に保存済みとする(嘘をつく) | 反映済 |
