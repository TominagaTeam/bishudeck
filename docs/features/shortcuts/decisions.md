---
feature: shortcuts
status: active
updated: 2026-08-26
---

# shortcuts — 判断ログ

蒸し返し防止用。ここは「まだ仕様に昇格していない決定」と「経緯」の置き場。仕様の正は design.md。
全体に影響する判断はここではなく [../../adr/](../../adr/) へ。

## 記録基準

- **書く**: 議論して決めたこと / 代替案を退けたこと / 後で「なぜこうした？」になりそうなこと
- **書かない**: 自明な判断、コードを見れば分かること

## 運用

- 判断が覆ったら行は消さず、状態を `上書き → #n` にする（覆った経緯こそ蒸し返し防止に効く）
- 判断が仕様として安定したら **design.md 本文に反映**し、状態を `反映済` にする

## ログ

| # | 日付 | 判断 | 理由 | 見送った代替案 | 状態 |
|---|---|---|---|---|---|
| 1 | 2026-08-22 | カタログを `src/shared/` に置く | `app` / `features` / `stage` の 3 者が読む。[05-directory](../../basic-design/05-directory.md) の「依存方向」を満たすのはここだけ | `app/` に置く（`stage` から逆流する）/ `core/` に置く（UI 表記は core の責務ではない） | 反映済 |
| 2 | 2026-08-22 | カタログはハンドラを持たない。実行は従来の 3 窓口のまま | ディスパッチまで集約すると、Escape の 4 段の優先順位や「入力欄では手を出さない」ガードの順序という**状態の判断**まで巻き込む。今回直したいのは「キーの定義が散っていること」だけ | 単一の keymap ディスパッチャに統合する | 反映済 |
| 3 | 2026-08-22 | 照合は寛容（`metaKey \|\| ctrlKey`）、表示は厳密（環境ごとに絞る） | 既存の `App.tsx` がそう動いており、当時の smoke の `pressKey` も Meta ビットしか送らなかった。狭めると既存の回帰テストが落ちる。一方で表記まで寛容にすると Windows に ⌘ が出る（今回直した不具合そのもの） | 環境ごとに照合も切り替える | 反映済 |
| 4 | 2026-08-22 | テキスト書式（⌘B / ⌘I / ⌘U）は割り当てない | 編集 iframe はスクリプト無効で、WebKit はその document のリスナを発火させない（`EditStage.tsx` の同趣旨のコメント）。テキストセッション中のキーはホスト側から捕まえられない。**現状はブラウザ標準の `contenteditable` 挙動がそのまま走る**。ただし 2026-08-23 の実測では、その結果はセッションを閉じる時点で `commitTextSession()` が拾い、「テキストを編集」コマンドとして履歴に入って Undo 1 回で戻せる([issues](../../issues.md) #2)。残るのは「⌘B 単体が 1 undo ステップにならない」「セッション中の ⌘Z がブラウザ側へ行く」の 2 点で、直すには iframe 側の設計変更が要る | ホストの capture で拾う（届かない）/ iframe に script を入れる（[ADR-0002](../../adr/0002-edit-preview-separation.md) 違反） | 反映済 |
| 5 | 2026-08-22 | ⌘M と ⌘⇧D を同じ「スライドを追加」に落とす | PowerPoint は Ctrl+M が「新しいスライド」、Ctrl+⇧D が「スライドの複製」。このアプリは白紙スライド追加を取り下げ済みで（[roadmap](../../roadmap.md)）、新規スライド＝隣の複製の 1 操作しかない。どちらのキーで探しても同じ場所に着く方が「操作を探さない」に近い | ⌘M だけ割り当てる / ⌘⇧D だけ割り当てる | 反映済 |
| 6 | 2026-08-22 | 修飾キーの表記順を Apple 標準（⌃⌥⇧⌘）に合わせ、`⌘⇧]` を `⇧⌘]` に改めた | 既存の表記は `⌥⌘C`（Apple 順）と `⌘⇧]`（逆）が混在していた。整形を 1 箇所に集める以上どちらかに倒す必要があり、プラットフォーム準拠を選んだ | 既存の `⌘⇧]` に揃える | 反映済 |
| 7 | 2026-08-22 | 環境判定は `navigator` を読む。`@tauri-apps/plugin-os` は使わない | ブラウザ（`npm run dev`）でも同じ答えが要る。プラグインだとフォールバックが必須になり、「自作 Rust コマンドの invoke は `backend.ts` に集約する」の例外も増える | `@tauri-apps/plugin-os` を追加する | 反映済 |
| 8 | 2026-08-22 | `MenuItem` の `shortcut` は表記ではなく **id** を受ける | 手書きのグリフを型エラーにできる。実際にこの変更で `Toolbar.tsx` に残っていた最後の `"⌘D"` がコンパイルエラーとして出た | `shortcutHint()` の戻り値を各呼び出し側で渡す | 反映済 |
| 9 | 2026-08-22 | 整列（左揃え等）にはキーを割り当てない | PowerPoint に既定キーが無く、独自に決めると [07-ui-system](../../basic-design/07-ui-system.md) の「独自の操作体系を発明しない」に反する | ⌥⌘← などを割り当てる | 反映済 |
| 10 | 2026-08-22 | スライドの並べ替え（⌘↑ / ⌘↓）とスライド削除キーは今回入れない | 範囲を「PowerPoint 実在キー」に絞る裁定。並べ替えは `MoveSlideCommand` があるので後から足せる | 今回まとめて入れる | [ideas](../../ideas.md) #7 |
| 11 | 2026-08-22 | ヘルプはドロップダウンを挟まず、ボタン 1 回で一覧を開く | 項目が 1 つしかないメニューは、何も選ばせずに 1 手増やすだけだった。将来「バージョン情報」等が増えたらメニューに戻す | ドロップダウンに畳む（最初はそう実装したが、開いてすぐ 1 項目しか無い） | 反映済 |
| 12 | 2026-08-26 | ⌘X / ⌘C / ⌘V を**要素**に割り当てる。書式のコピー / 貼り付け(⌥⌘C・⌥⌘V / Ctrl+⇧C・Ctrl+⇧V)とは**修飾キー 1 つ**で分ける | #10 と同じ「PowerPoint 実在キーだけ」の線で、この 3 つは PowerPoint にも同じ意味である。カタログは名指ししない修飾キーの不在を要求する(#3)ので、⌘C と ⌥⌘C・Ctrl+C と Ctrl+⇧C は取り違えようがない —— そこを回帰テストで固定した | 書式側を別のキーへ動かす(先にあったのは書式のほうで、動かす理由が無い) | 反映済 |
| 13 | 2026-08-26 | ショートカット一覧の**リード文を置かない** | 「PowerPoint と同じキーに合わせてあります」と出していたが、実際には PowerPoint に無いキー(⌘/・Tab での兄弟送り)も、割り当てが違うキー(⌘M。#5)もある。**一覧そのものが答え**なので、当たっていない前置きを足さない | 文面を正確に書き直す(「おおむね合わせています」は何も伝えない) | 反映済 |
