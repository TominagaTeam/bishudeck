---
feature: theme
status: active
updated: 2026-08-26
---

# theme — 判断ログ

蒸し返し防止用。ここは「まだ仕様に昇格していない決定」と「経緯」の置き場。仕様の正は [design.md](design.md)。
全体に影響する判断はここではなく [../../adr/](../../adr/) へ。

## 記録基準

- **書く**: 議論して決めたこと / 代替案を退けたこと / 後で「なぜこうした？」になりそうなこと
- **書かない**: 自明な判断、コードを見れば分かること

## 運用

- 判断が覆ったら行は消さず、状態を `上書き → #n` にする（覆った経緯こそ蒸し返し防止に効く）
- 判断が仕様として安定したら **design.md 本文に反映**し、状態を `反映済` にする

## ログ

| # | 日付 | 判断 | 理由 | 見送った代替案 | 状態 |
|---|---|---|---|---|---|
| 1 | 2026-08-26 | 既定を `light` にし、`system` は 3 つ目の選択肢に留める | 編集対象のスライドが白いので、既定のアプリも白いほうが地続き。ユーザーの指示も「デフォルトはライト」 | 既定を `system` にする（表示言語の解決順に揃う）。**OS がダークの人に、指示と違う既定を配ることになる**ので退けた | 反映済 |
| 2 | 2026-08-26 | `data-theme` には解決済みの `light` / `dark` だけを書く | `system` を属性に出すと、スタイルシートがダークの全トークンを `@media (prefers-color-scheme: dark)` の中にもう一度持つことになる。カスタムプロパティはメディアクエリを跨いでまとめられない | `data-theme='system'` + メディアクエリ。**同じ 20 個のトークンが 2 箇所**になり、片方だけ直す事故が必ず起きる | 反映済 |
| 3 | 2026-08-26 | `--accent` と `--brand`、オーバーレイの色リテラルはテーマで動かさない | どれも**スライドの上に描く**もので、明暗は「デッキの地色に対して読めるか」で決まっている。アプリの配色設定で選択枠の色が変わるのは筋が違う | 配色ごとに accent を変える。ステージの見た目がアプリ設定に連動してしまう | 一部上書き → #10 |
| 4 | 2026-08-26 | **面として塗る青**だけ `--accent-fill` に分ける | ライトで `#3884ff` を塗ると白文字が **3.55:1**（実測）。塗るのは `button.active` と `.modal-actions .primary` の 2 箇所だけで、線として使う 19 箇所は据え置ける。ライトは `#2563eb` で **5.17:1** | ① `--accent` 自体をライトで濃くする → #3 に反する。② 塗りの上の文字を `--text` にする → 青地に黒文字で UI の慣習から外れる | 反映済 |
| 5 | 2026-08-26 | 切り替え口はステータスバー右端（ズームの左） | テーマもズームもペイン幅も「ワークスペースの見え方」で、**コマンドではない**。ツールバーは 5 群がコマンドとモードで埋まっている | ① ツールバー右端 → 文字ラベル必須の方針（[07-ui-system](../../basic-design/07-ui-system.md)）で幅を食う。② 設定ダイアログを新設 → 今回の変更量に対して重い。将来 言語切替（[issues](../../issues.md) #15）と同居させるなら再検討 | 反映済 |
| 6 | 2026-08-26 | テーマの保存は `uiStore` の debounce キューを通さない | 好みは 1 クリックに 1 回しか届かず、しかも**次のフレームまでに `<html>` へ出ていないと**画面が古い配色のまま残る。ペイン幅は毎フレーム届くので debounce が要る、という事情が無い | キューに相乗り。`pagehide` の flush は付いてくるが、200ms 後まで属性が付かない | 反映済 |
| 7 | 2026-08-26 | サムネイルに `--border` の 1px を足す | ライトではペインが白で、サムネイルの白（スライドの地）と溶ける。**枠は chrome 側の色**なので、スライドの `#fff` は据え置いたまま外から縁だけ足せる | サムネイルの背景をテーマに追従させる → **スライドの地色を偽る**ことになり、不変条件 2 の趣旨に反する | 反映済 |
| 8 | 2026-08-26 | `MediaQueryList` をモジュールスコープの `Set` で握る | 監視の解除関数を呼び出し側が捨てるので、`MediaQueryList` への強参照が消える。仕様上は document が保つべきだが、**WKWebView で外れると `system` だけが黙って動かなくなる** | 何もしない。失敗しても症状が出ないので、出たときに原因に辿り着けない | 反映済 |
| 9 | 2026-08-26 | 配色を暖色ニュートラルから**寒色スレート**へ入れ替える。ライトの `--panel` は `#fff` をやめ、`--canvas-bg` を深くする | 暖色クリーム + テラコッタが Anthropic のブランドカラーに寄りすぎていて、プロダクト自身の個性として立たない。あわせて**画面で一番明るいものをスライドそのものにした** —— 純白のペインは白いスライドと同じ明度で並び、chrome が編集対象と明るさを取り合っていた | ① 暖色のまま彩度だけ落とす → 寄りすぎの原因は色相なので効かない。② ライトだけ寒色にしてダークは暖色のまま → 同じキャンバスに並べると別プロダクトに見える | 反映済 |
| 10 | 2026-08-26 | `--brand` を**配色追従**にし(ライト = インク / ダーク = 反転白)、未保存ドットを `--dirty`(琥珀)へ切り出す | `--brand` はツールバーのボタンで、**スライドの上には一度も描かれない** —— #3 が「動かさない」根拠に挙げた「デッキの地色に対して読めるか」がそもそも当てはまらない。色相を与えないのは、青が「選ばれている」で埋まっている以上、バーの上の 2 つ目の色相が必ず青と喧嘩するため。ドットを分けたのは、brand が片方でインク・もう片方でほぼ白になり、**どちらも「未処理のものがある」とは読めない**から | ① brand を据え置いて色だけ変える(深い藍など) → 選択の青と色相が近く、同じ画面で 2 つの青が競合する。② ドットも brand に追従させる → ダークでただの白い点になり、装飾と区別が付かない | 反映済 |
| 11 | 2026-08-26 | chrome 用の書体は**同梱しない**(`system-ui` 据え置き) | モックは Instrument Sans + JetBrains Mono で組んだが、Tauri はオフラインなので Google Fonts が引けず、採るなら [ADR-0010](../../adr/0010-bundle-two-typefaces.md) と同じ同梱になる。**ユーザーの指示が「フォントは今のまま」**。配色と幾何だけで狙いは足りる | chrome 用に 2 書体を同梱(+200KB 程度)。スライドのための同梱とは目的が違い、**chrome の見た目のためだけに**バンドルを増やすことになる | 反映済 |
